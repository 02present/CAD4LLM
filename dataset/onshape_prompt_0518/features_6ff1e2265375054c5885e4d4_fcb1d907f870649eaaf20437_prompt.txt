Create a base shape by sketching four lines on the XY plane within a 400mm wide and 300mm tall bounding box, then extrude it 20mm to form a solid body. Next, sketch sixteen lines on the XY plane within a slightly larger bounding box and extrude this new sketch 200mm to create a second separate solid body.
