Create a base shape by extruding a sketch on the XY plane with four lines, then add two more extrusions on different sketches to form the main structure. Apply a fillet with a 2mm radius and drill a hole, followed by removing material with another extrusion. Finally, combine all bodies using a boolean operation.
