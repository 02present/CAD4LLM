Create a base shape by extruding a sketch on the XY plane with four lines, then add several more extrusions using different sketches and distances, including one that removes material. Finally, combine all the bodies using a boolean operation.
