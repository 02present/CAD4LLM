Create a base shape by sketching on the XY plane, then extrude it 5 millimeters to form the first body. Add another extrusion of 13 millimeters to extend the shape further, ensuring both extrusions use the same sketch reference. Finally, apply an external thread to the threaded feature.
