Create a base shape by extruding a sketch on the XY plane with a 62 mm depth, then add a chamfer to the edge. Next, create a second sketch on the same plane but larger in height and extrude it 31 mm to form a separate feature.
