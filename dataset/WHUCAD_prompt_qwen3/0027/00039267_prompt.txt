Create a base shape by extruding a circle with an 8 mm height, then add another identical extrusion on top. Next, create a smaller circular extrusion with a 4 mm height centered towards the top. Finally, sketch and extrude a rectangular profile across the entire width to form the final structure.
