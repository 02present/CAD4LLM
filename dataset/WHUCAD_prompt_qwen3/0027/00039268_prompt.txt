Create a base body by extruding a circle sketch on the XY plane 21 mm upwards. Then, chamfer the edge. Add a second circular sketch on the XY plane within a smaller bounding box and extrude it 4 mm upwards to form a separate feature.
