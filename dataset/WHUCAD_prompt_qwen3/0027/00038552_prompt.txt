Create a base shape by extruding a square sketch along the Z-axis for 96 millimeters. Then, create a symmetrical revolved feature on top of the base using another square sketch in the XY plane. Ensure both sketches are aligned and properly referenced for accurate construction.
