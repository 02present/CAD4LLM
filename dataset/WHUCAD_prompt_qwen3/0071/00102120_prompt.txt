Create a base shape by extruding a sketch on the XY plane with four lines, then chamfer the edges. Add a second extrusion using a smaller sketch on the XY plane with three lines, making sure to keep the dimensions distinct and aligned as specified.
