Create a base shape by extruding a sketch on the XY plane with a 64 mm depth, then add a chamfer to the edge. Next, create a second sketch on the same plane but slightly larger and extrude it with a 32 mm depth to form a separate body.
