Create a base shape by extruding a series of sketched profiles along the Z-axis with a distance of 53 mm, then apply chamfers to selected edges and repeat similar extrusions using different sketched circles and lines on the XY plane.
