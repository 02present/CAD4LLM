Create a base shape by sketching three arcs and lines on the XY plane with a bounding box of 56mm by 95mm, then extrude it 28mm to form a solid body. Add a chamfer to the edge, and create a circular sketch on the XY plane within a smaller bounding box, then extrude that circle 28mm to form a second separate body.
