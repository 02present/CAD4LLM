Create a base shape by extruding a sketch on the XY plane with four lines, then chamfer the edges. Next, create a second extrusion using a new sketch with three lines on the XY plane, extruding it 10 millimeters to form a smaller feature.
