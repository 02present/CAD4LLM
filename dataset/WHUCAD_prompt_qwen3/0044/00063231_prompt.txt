Create a base shape by sketching four lines on the XY plane forming a square with a 95 mm bounding box, then extrude it 24 mm to form a new body. Repeat the sketch and extrusion process to create a second identical body, then chamfer the edges where they meet.
