Create a base shape by extruding a sketch on the XY plane with a 76mm by 95mm bounding box, then add two chamfers to the edges. Next, create a smaller extrusion based on another XY sketch with a 95mm by 48mm bounding box, and apply a final chamfer to complete the design.
