Create a base body by extruding a circle with a 96 mm diameter along the Z-axis. Then, chamfer the edge of the base body. Next, create a second circular sketch on the XY plane with a smaller diameter and extrude it 126 mm to form a second protrusion.
