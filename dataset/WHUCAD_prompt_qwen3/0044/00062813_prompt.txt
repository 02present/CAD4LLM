Create a base shape by extruding a sketch of four lines on the XY plane with a 48x95 mm bounding box, then add a chamfer. Next, create a second extrusion using a new sketch of four lines on the same plane but with a 95x95 mm bounding box, extruding it 48 mm to form a separate body.
