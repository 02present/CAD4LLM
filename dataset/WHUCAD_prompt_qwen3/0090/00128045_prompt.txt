Create a base shape by extruding a circle from the first sketch 32 millimeters, then add a chamfer. Next, create a larger circular sketch centered on the XY plane and extrude it 32 millimeters to form a second body. Repeat the process with an even larger circle and extrude it 64 millimeters, finishing with a final chamfer on the top edge.
