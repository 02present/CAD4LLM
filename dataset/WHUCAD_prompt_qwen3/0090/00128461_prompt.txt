Create a base shape by sketching five lines on the XY plane with a bounding box from [0.0, -95.0, 15.0, 10.0], then extrude it 1 mm to form a new body. Add a chamfer to the edge, followed by another sketch with eight lines on the XY plane within a smaller bounding box, extrude that as well, and apply two more chamfers to the final edges.
