Create a base shape by extruding a sketch consisting of three lines on the XY plane, then add another extrusion using a second sketch with five lines on the same plane but shifted downward. Finally, chamfer two edges of the resulting model.
