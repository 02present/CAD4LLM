Create a base shape by extruding a sketch on the XY plane with a 20 mm depth, then add a chamfer to the edge. Next, create a second sketch slightly smaller than the first and extrude it 20 mm to form a secondary feature.
