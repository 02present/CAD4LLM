Create a base shape by extruding a sketch on the XY plane with seven lines, then create another extrusion using a different sketch with six lines, both extending 26 millimeters. Add a chamfer to one of the edges, and continue by extruding additional sketches on the XY plane with fewer lines, each also extending 26 millimeters to form a series of connected features.
