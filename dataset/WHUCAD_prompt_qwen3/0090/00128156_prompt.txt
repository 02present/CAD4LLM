Create a base shape by extruding a series of circles along the Z-axis with a 4mm depth, then add a chamfer to one edge. Next, create a larger central extrusion using a circle with a 17mm depth, and repeat this pattern around the center to form a symmetrical structure.
