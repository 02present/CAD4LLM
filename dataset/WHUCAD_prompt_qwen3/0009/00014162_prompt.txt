Create a base shape by extruding a sketch on the XY plane with five lines, then chamfer the edges. Add a second extruded feature using a smaller sketch on the same plane to form a secondary structure.
