Create a base shape by extruding a sketch on the XY plane with a 64 mm depth, then add two chamfers to the edges. Next, create a smaller sketch on the same plane and extrude it 32 mm to form a secondary feature.
