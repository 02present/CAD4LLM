Create a base shape by extruding a sketch on the XY plane with a 95x83mm bounding box, then chamfer the edges. Next, create a second extrusion using a new sketch on the XY plane with a 91x95mm bounding box and extrude it 8mm.
