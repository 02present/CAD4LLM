Create a base shape by extruding a circle with a diameter of 16 mm along the Z-axis. Then, add a chamfer to the edge. Next, create a larger circle and extrude it 24 mm to form a second feature. Finally, create an even larger circle and extrude it 24 mm to complete the model.
