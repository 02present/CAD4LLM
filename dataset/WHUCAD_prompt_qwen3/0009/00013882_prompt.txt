Create a base shape by extruding a sketch on the XY plane with a 64 mm depth. Then, add a chamfer to the edge. Next, create a larger extrusion using a new sketch on the XY plane with a 96 mm depth. Finally, add another extrusion using a smaller sketch on the XY plane with a 64 mm depth.
