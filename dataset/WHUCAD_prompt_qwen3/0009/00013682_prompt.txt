Create a base shape by extruding a sketch on the XY plane with a 90 mm height, then add a chamfer to the edge. Next, create a second extrusion of 15 mm height using a new sketch on the same plane but with a smaller bounding box.
