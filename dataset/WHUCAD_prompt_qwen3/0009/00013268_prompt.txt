Create a base body by extruding a sketch with six lines on the XY plane, then create two more bodies by extruding separate sketches with four lines each, all at a distance of 64 millimeters. Ensure each extrusion is blind and based on its respective sketch.
