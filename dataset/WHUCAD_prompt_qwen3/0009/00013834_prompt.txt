Create a base shape by extruding a sketch on the XY plane with four lines, then chamfer the edges. On the same plane, create a second sketch with three lines positioned lower and extrude it to form a second body, ensuring both extrusions are 64 millimeters deep.
