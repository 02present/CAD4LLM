Create a base shape by extruding a sketch on the XY plane with a 95x63 mm bounding box, then add a chamfer. Next, create a second extrusion using a larger XY sketch with a 95x80 mm bounding box, also extruded 64 mm deep.
