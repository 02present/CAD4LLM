Create a base shape by extruding a sketch on the XY plane with a 79mm by 95mm bounding box, then add a second extrusion using a smaller sketch on the same plane with a 95mm by 48mm bounding box, followed by a chamfer on the edges, and finally create a third extrusion using an even smaller sketch positioned lower on the XY plane with a 95mm by 11mm bounding box.
