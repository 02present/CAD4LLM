Create a base shape by extruding a sketch with five lines on the XY plane to a depth of 64 mm. Then, chamfer the edges. Next, create a larger sketch with six lines on the same plane and extrude it to 32 mm. Finally, repeat the process with another smaller sketch and extrude it to 32 mm, and chamfer the resulting edges as well.
