Create a base shape by extruding a sketch along the Z-axis for 59 millimeters, then create another extrusion of 96 millimeters from a second identical sketch, and finally chamfer the edges where they meet.
