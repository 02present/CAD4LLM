Create a base shape by extruding a sketch on the XY plane with a 12 mm depth, then add a chamfer to the edge. Next, create a second sketch slightly smaller than the first and extrude it 7 mm to form a secondary feature.
