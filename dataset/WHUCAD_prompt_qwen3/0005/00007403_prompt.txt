Create a base shape by extruding a sketch on the XY plane with five lines, then chamfer the edges. Add another extruded feature using a second sketch with six lines on the same plane but shifted downward, ensuring both extrusions are 96 millimeters deep.
