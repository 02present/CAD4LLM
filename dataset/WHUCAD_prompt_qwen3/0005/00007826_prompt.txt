Create a base shape by sketching a circle on the XY plane with a bounding box from 11 to 85 in X and -37 to 37 in Y, then extrude it 84 mm to form a solid body. Next, chamfer the edges. Then sketch four lines on the XY plane within a 95x12 mm bounding box and extrude them 10 mm to create a separate feature.
