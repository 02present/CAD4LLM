Create a base shape by extruding a sketch on the XY plane with a 62x95mm bounding box 105mm upwards. Then chamfer the edges. Add a smaller sketch on the same plane but with a 10x95mm bounding box and extrude it 93mm upwards to form a separate feature.
