Create a symmetrical part by first sketching three lines on the XY plane forming a triangular shape, then revolving that sketch around the Z-axis to create a conical surface. Next, sketch another identical triangle on the XY plane and extrude it 90 millimeters along the Z-axis to form a separate body. Finally, combine both features into a single assembly.
