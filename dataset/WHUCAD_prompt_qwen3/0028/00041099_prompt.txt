Create a base shape by sketching four lines on the XY plane within a 86x95 mm bounding box, then revolve it around an axis to form a symmetrical 3D object. Next, sketch a circle on the XY plane within a 24x24 mm bounding box and extrude it 34 mm to create a separate protruding feature.
