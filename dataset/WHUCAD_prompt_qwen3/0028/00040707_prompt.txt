Create a base body by extruding a sketch along the XY plane with a 96 mm depth, then add two chamfers to the edges. On the same plane, sketch another shape including an arc and extrude it 32 mm to form a secondary feature.
