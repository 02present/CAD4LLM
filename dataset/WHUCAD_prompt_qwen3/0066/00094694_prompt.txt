Create a base shape by extruding a sketch of four lines on the XY plane to a depth of 96 mm. Then chamfer the edges. On the same plane, sketch a circle centered and extrude it to a depth of 66 mm to form a separate feature.
