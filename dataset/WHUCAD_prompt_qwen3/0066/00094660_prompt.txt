Create a base shape by extruding a sketch on the XY plane with a 48 mm depth, then add two chamfers to the edges. Next, create a second sketch on the same plane with three lines and extrude it 104 mm to form a separate body.
