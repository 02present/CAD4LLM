Create a base shape by extruding a sketch along the Z-axis for 56 mm, then add two chamfers to the edges. Repeat the process using the same sketch to create two more identical extrusions stacked vertically, each 56 mm tall. Ensure all sketches are aligned on the XY plane with consistent dimensions.
