Create a base shape by extruding a sketch on the XY plane with a 72 mm depth, then add a chamfer to the edge. Next, create a second extrusion using a slightly smaller sketch on the same plane with a 78 mm depth to form the final structure.
