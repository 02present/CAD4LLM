Create a base body by extruding a sketch along the Z-axis for 80 mm, then create a second identical body stacked on top of the first, and finally add a smaller protrusion by extruding another sketch 4 mm upwards.
