Create a base shape by extruding a sketch consisting of five lines on the XY plane with a width of 9mm and length up to 95mm, then chamfer the edges. Next, create a second extrusion using a separate sketch of four lines on the XY plane with dimensions up to 95mm by 18mm, extruding it 96mm to form a separate body.
