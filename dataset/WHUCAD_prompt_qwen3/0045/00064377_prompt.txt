Create a base shape by extruding a sketch on the XY plane with seven lines, then add another extrusion using a different sketch with six lines, followed by chamfers on two edges. Continue by creating more extrusions with varying heights and sketches, ensuring each new feature is built on the XY plane with appropriate dimensions and line entities.
