Create a base shape by sketching four lines on the XY plane within the specified bounding box, then extrude it 81 millimeters to form a solid body. Apply chamfers to two of the edges to refine the corners.
