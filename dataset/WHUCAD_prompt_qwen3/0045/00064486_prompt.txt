Create a base shape by sketching a series of lines on the XY plane with a bounding box from 0 to 95 in X and -55 to 40 in Y, then extrude it 24 millimeters to form a solid body. Add a chamfer to one edge, and create a second sketch on the same plane with a square bounding box, extruding it 5 millimeters to form a separate feature.
