Create a base shape by extruding a sketch along the XY plane for 53 millimeters, then add a chamfer to the edge. Next, create a second identical sketch and extrude it for 33 millimeters to form a separate body.
