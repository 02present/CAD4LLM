Create a base shape by extruding a sketch on the XY plane with a 95x64mm bounding box, then chamfer the edges. Add a second extrusion of a smaller sketch on the same plane with a 95x53mm bounding box, extending 127mm in height.
