Create a base shape by extruding a sketch on the XY plane with a 95x35mm bounding box, then chamfer the edges. Next, create a second extrusion using a new sketch on the XY plane with a 45x95mm bounding box and extrude it 61mm.
