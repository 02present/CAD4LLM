Create a base shape by extruding a sketch on the XY plane with a 46mm by 95mm bounding box, then chamfer the edges. Add a second extruded feature using a smaller sketch on the same plane but with a 7mm width, both extruded 96mm in length.
