Create a base shape by extruding a sketch on the XY plane with a 30x95mm bounding box, then repeat the same sketch and extrusion process twice more to form three identical bodies stacked vertically. Add two chamfers to the edges of the first body and create a final extruded feature using a larger sketch with a 95x36mm bounding box and a 100mm extrusion depth.
