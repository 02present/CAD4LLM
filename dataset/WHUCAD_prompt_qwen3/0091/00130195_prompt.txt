Create a base shape by extruding a sketch on the XY plane with a 11 mm depth. Then, add three chamfers to the edges. On the same plane, create two more sketches and extrude them with a 76 mm depth each, finally applying a chamfer to the last feature.
