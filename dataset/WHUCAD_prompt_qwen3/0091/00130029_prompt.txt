Create a base shape by extruding a sketch on the XY plane with six lines, then add another extruded feature using a smaller sketch with four lines, and finally chamfer the edges of the resulting model.
