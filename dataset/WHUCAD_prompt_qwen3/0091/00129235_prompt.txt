Create a base shape by extruding a sketch on the XY plane with a 36mm by 95mm bounding box, then extrude another sketch with a 95mm by 71mm bounding box to form a second body. Add three chamfers to the edges of the final assembly, ensuring all extrusions are set to a depth of 52mm except the last one, which should be 22mm.
