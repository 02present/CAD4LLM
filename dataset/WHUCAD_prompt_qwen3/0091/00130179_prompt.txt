Create a base shape by extruding a sketch on the XY plane with four lines, then repeat this process using different sketches on the same plane to form multiple protrusions. Add chamfers to the edges and create additional extruded features based on new sketches to build the final complex structure.
