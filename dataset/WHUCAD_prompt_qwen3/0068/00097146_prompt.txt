Create a base shape by extruding a sketch consisting of four lines on the XY plane with a bounding box of 95mm by 57mm, then chamfer the edges. Next, create a second extrusion using an arc and line sketch on the same plane but with a zero height dimension, and extrude it 20mm to form a separate feature.
