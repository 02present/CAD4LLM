Create a base shape by extruding a sketch on the XY plane with four lines, then chamfer the edges. Add a second extrusion using an expanded sketch with eight lines on the same plane to form the final structure.
