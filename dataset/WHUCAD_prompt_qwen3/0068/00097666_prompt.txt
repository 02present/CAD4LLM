Create a base body by extruding a circle sketch on the XY plane 96 mm upwards, then chamfer the edges. Add a second circular sketch slightly smaller and offset on the same plane, and extrude it 80 mm upwards to form a separate feature.
