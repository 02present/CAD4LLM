Create a base shape by extruding a sketch consisting of four lines on the XY plane with a bounding box of 95x83 mm, then chamfer the edges. Next, create a second extrusion using an arc and line sketch on the same plane but with a zero height in the Y-direction, and extrude it 54 mm to form a separate body.
