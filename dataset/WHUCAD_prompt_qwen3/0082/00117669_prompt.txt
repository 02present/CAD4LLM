Create a base shape by extruding a sketch on the XY plane with a 96 mm depth. Then, chamfer the edges of the resulting body. Next, create a second extrusion using a smaller sketch on the XY plane, extruding it 48 mm deep to form a separate feature.
