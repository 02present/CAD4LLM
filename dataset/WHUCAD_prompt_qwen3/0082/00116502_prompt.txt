Create a base shape by extruding a sketch on the XY plane with a 95x93 mm bounding box, then chamfer the edges. Add a second extruded feature using a slightly smaller sketch on the same plane, both extending 73 mm in the same direction.
