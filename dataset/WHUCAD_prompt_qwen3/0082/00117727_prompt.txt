Create a base shape by extruding a sketch with four lines on the XY plane, 72 mm deep. Then chamfer the edges. Add a second sketch with five lines on the same plane but taller, and extrude it 104 mm to form a separate body.
