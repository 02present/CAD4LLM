Create a base shape by sketching four lines on the XY plane within a 95x76 mm bounding box, then extrude it 29 mm to form a solid body. Add a chamfer to the edge, and create a circular sketch centered on the XY plane with a 96x96 mm bounding box, then extrude it 124 mm to form another separate body.
