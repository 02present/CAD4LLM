Create a base body by extruding a sketch along the XY plane for 51 mm, then add a chamfer to the edge. On the same plane, create a second sketch with smaller dimensions and extrude it 42 mm to form a separate feature.
