Create a base body by extruding a sketch on the XY plane with a 76x95mm bounding box 96mm upwards. Then, add three chamfers to the edges. On the same plane, create two more sketches with 95x36mm bounding boxes and extrude each 96mm upwards. Finally, apply additional chamfers to the resulting features.
