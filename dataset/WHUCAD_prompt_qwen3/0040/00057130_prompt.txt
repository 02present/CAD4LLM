Create a base shape by extruding a sketch on the XY plane with a 32 mm depth, then add a chamfer to the edge. Repeat this process four times using different sketches, each time adjusting the sketch dimensions slightly to form a series of connected extruded features.
