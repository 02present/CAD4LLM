Create a base shape by sketching three lines on the XY plane within the specified bounding box, then extrude the sketch 11 millimeters to form a new body. Apply chamfers to two of the edges to refine the corners.
