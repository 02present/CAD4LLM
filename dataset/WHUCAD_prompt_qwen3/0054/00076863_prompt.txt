Create a base shape by extruding a sketch on the XY plane with a 95x71mm bounding box, then chamfer the edges. Next, create a second extrusion using a rotated sketch on the XY plane with a 66x95mm bounding box, extruding it 59mm to form a separate body.
