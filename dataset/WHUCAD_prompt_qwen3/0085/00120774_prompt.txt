Create a base shape by extruding a sketch on the XY plane with seven lines, then chamfer the edges. Add two more extrusions based on subsequent sketches on the same plane, each forming a smaller profile, resulting in three distinct extruded features.
