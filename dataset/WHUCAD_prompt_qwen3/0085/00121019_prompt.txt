Create a base feature by extruding a small circle with a 1 mm height, then chamfer the edge. Next, create five identical extrusions of larger circles, each extending 125 mm in height, aligned along the same axis.
