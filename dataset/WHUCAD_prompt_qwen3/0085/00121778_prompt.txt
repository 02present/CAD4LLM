Create a base shape by extruding a sketch on the XY plane with a 95x50mm bounding box, then extrude another sketch on the XY plane with a 25x95mm bounding box to form a symmetrical structure, and finally chamfer the edges where they meet.
