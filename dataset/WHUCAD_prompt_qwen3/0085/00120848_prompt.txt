Create a base shape by extruding a sketch on the XY plane with a 95mm by 74mm bounding box, then chamfer the edges. Add a second extrusion using a smaller sketch on the XY plane with a 37mm by 95mm bounding box, extruding it 99mm to form the final part.
