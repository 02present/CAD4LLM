Create a base shape by extruding a sketch on the XY plane with a 95x67mm bounding box, then chamfer the edges. Add a second extruded feature based on a smaller sketch with a 95x50mm bounding box, extruding 51mm to form a separate body.
