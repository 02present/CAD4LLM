Create a base shape by extruding a sketch on the XY plane with five lines spanning 76mm wide and 95mm tall, then chamfer the edges. Next, create a second extrusion using a smaller sketch on the XY plane with three lines, positioned lower, and extrude it 96mm to form a separate body.
