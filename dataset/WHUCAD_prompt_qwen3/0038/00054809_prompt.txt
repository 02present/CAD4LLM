Create a base body by extruding a circle with a 24 mm diameter along the Z-axis. Then, create a second larger body by extruding a circle with a 96 mm diameter along the Z-axis, ensuring both features are separate and distinct.
