Create a base body by extruding a sketch on the XY plane with four lines, then create a second body by extruding another sketch on the same plane with four lines, both extending 25 mm. Add a chamfer to the corner where the two bodies meet. Finally, extrude a third sketch with eight lines on the XY plane to form the top feature, also extending 25 mm.
