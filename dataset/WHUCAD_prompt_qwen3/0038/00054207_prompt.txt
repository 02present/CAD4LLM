Create a base shape by extruding a sketch with four lines on the XY plane, then chamfer the edges. Add a second extrusion using a smaller sketch with three lines on the same plane, extending it 64 mm to form a separate body.
