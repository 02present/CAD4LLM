Create a base shape by extruding a sketch on the XY plane with four lines, then chamfer the edges. Repeat this process with different sketches, varying the extrusion distances and adding additional features like arcs and lines in subsequent sketches, and extrude each to form separate bodies. Finally, apply chamfers to the final extruded features.
