Create a base shape by extruding a sketch on the XY plane with dimensions 76mm by 95mm, then add a chamfer. Next, create two additional features by extruding smaller sketches on the same plane, each with a height of 51mm.
