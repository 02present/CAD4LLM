Create a base shape by extruding a sketch on the XY plane with a 90mm width and 48mm height, then chamfer the edges. Add a second extrusion on the same plane but slightly larger, extending 11mm to form a separate feature.
