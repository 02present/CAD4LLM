Create a base shape by extruding a sketch with four lines on the XY plane, then add two more extrusions using subsequent sketches with similar dimensions but slightly larger bounding boxes, and finally create a taller extrusion using a sketch with eight lines on the same plane.
