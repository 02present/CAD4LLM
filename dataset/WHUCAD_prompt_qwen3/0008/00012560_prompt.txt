Create a base body by extruding a circle sketch in the XY plane 13 millimeters blind. Then chamfer the edge. Add a second smaller circle sketch slightly offset and extrude it 3 millimeters to form a secondary feature.
