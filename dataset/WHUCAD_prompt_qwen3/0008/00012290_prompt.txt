Create a base shape by extruding a sketch on the XY plane with six lines, then create another extrusion from a similar sketch with four lines, both extending 47 mm. Add chamfers to three selected edges and repeat the extrusion process using additional sketches with varying dimensions, ensuring all features align properly.
