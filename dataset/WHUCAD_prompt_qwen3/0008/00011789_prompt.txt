Create a base shape by extruding a sketch along the Z-axis for 32 mm, then add a chamfer to the edge. On the same plane, create another identical sketch and extrude it 32 mm to form a second body, ensuring both features are aligned and proportionally sized.
