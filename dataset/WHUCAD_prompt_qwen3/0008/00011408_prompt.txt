Create a base body by extruding a sketch on the XY plane with four lines, then add five more extrusions using subsequent sketches on the same plane, each with different dimensions and line counts. Finally, apply chamfers to two specific edges.
