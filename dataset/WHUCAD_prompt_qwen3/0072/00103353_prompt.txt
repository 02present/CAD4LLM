Create a base shape by extruding a sketch on the XY plane with a 95x73mm bounding box 1mm. Then, create two additional protrusions by extruding smaller sketches on the XY plane with a 5x95mm bounding box, each 6mm in height.
