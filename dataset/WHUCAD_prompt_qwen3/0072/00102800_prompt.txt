Create a base body by extruding a sketch on the XY plane with a 45x95mm bounding box, then chamfer the edges. Add a second extruded feature using a smaller sketch on the same plane with a 52x78mm bounding box, both extending 3mm.
