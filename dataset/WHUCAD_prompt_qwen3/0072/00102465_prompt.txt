Create a base shape by sketching a circle on the XY plane and extruding it 125 mm to form a cylinder. Then, sketch four lines on the XY plane to create a frame-like structure and extrude it 11 mm to form a separate block. Finally, chamfer the edges where the two parts meet.
