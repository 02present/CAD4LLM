Create a base shape by extruding a sketch consisting of five lines on the XY plane, then chamfer the edges. Next, create a circular sketch on the XY plane and extrude it to form a second body, ensuring both features are distinct and properly positioned.
