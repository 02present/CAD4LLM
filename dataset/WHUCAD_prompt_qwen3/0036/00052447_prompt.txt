Create a base shape by extruding a sketch on the XY plane with four lines, then create another extrusion using a different sketch with six lines, and finally add a third extrusion from a smaller sketch also on the XY plane. Apply chamfers to two specific edges to refine the corners of the final design.
