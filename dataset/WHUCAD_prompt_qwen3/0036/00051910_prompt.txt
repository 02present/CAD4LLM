Create a base shape by extruding a sketch on the XY plane with a 49 mm depth, then add two chamfers to the edges. Next, create a smaller protrusion by extruding another sketch on the same plane with a 25 mm depth.
