Create a base shape by sketching 12 connected line and arc segments on the XY plane with a bounding box from [0.0, -55.0] to [79.0, 24.0], then extrude this sketch 14 millimeters to form a solid body. Add five circular sketches on the same plane at specified positions and sizes, each representing a separate feature.
