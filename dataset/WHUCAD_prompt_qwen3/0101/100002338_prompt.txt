Create a base body by extruding a circle with a 96mm diameter along the Z-axis for 30mm. Then, create a second body by extruding another circle with a 67mm diameter along the Z-axis for 15mm. Add six lines arranged in a rectangular pattern across the XY plane to form a frame structure.
