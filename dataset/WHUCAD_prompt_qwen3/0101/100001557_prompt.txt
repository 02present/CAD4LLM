Create a base shape by sketching an arc, line, arc, line, arc, and line on the XY plane within the specified bounds, then extrude it 3 mm to form a solid body. Next, sketch a circle on the XY plane with a larger bounding box and use it as a reference for a new sketch. Finally, add a second sketch with lines and arcs on the same plane to define additional features.
