Create a base shape by sketching four lines on the XY plane forming a square with a 95mm side length, then extrude it 1mm to form a solid. Next, create a smaller circular sketch centered at 22mm from the origin with a 52mm diameter and extrude it 3mm to make a separate feature. Add two more similar circular sketches slightly offset and extrude them 3mm each. Finally, chamfer the edges of the main body and the smaller features with a 1mm chamfer.
