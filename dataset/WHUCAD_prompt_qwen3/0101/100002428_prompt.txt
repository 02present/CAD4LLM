Create a base shape by sketching six lines on the XY plane within the specified bounds, then extrude it 14 millimeters to form a solid body. Next, sketch a circle on the same plane with a larger bounding box and use it as a reference for any subsequent operations.
