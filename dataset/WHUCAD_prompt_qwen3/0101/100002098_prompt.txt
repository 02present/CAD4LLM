Create a base feature by extruding a circle with a diameter of 96 mm along the Z-axis for 19 mm. Then, add a smaller circular sketch in the same plane and extrude it as a separate body. Finally, create two additional circular sketches with smaller diameters and extrude them as well.
