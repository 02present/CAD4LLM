Create a base shape by extruding a sketch with four lines on the XY plane, then add additional features by extruding other sketched profiles at specified distances, including circular sketches for holes, ensuring all dimensions and positions match the provided coordinates and sizes.
