Create a base feature by extruding a sketch consisting of two lines and an arc on the XY plane, with a depth of 21 millimeters. Then, create another sketch on the same plane with similar elements and use it to form a new body. Ensure both sketches are positioned within the specified bounding box dimensions.
