Create a base shape by sketching four lines on the XY plane forming a square with a 95mm side length, then extrude it 5mm to create a solid body. Next, sketch a smaller circle centered at 70mm on the XY plane and extrude it to form a separate feature. Finally, add a larger circular sketch spanning from -48mm to 96mm on the Y-axis and use it as a reference for further operations.
