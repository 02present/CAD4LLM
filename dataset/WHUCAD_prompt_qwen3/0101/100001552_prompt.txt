Create a base shape by sketching six lines and two arcs on the XY plane, then extrude it 40 millimeters to form a solid body. Next, create a new sketch on the same plane with three lines to define a secondary feature.
