Create a base shape by sketching a series of lines on the XY plane and extruding them to form a solid body. Add chamfers to two corners, then create additional features by sketching and extruding new shapes at different depths to build the final model. Ensure all sketches are properly referenced and dimensions match the specified distances.
