Create a new body by extruding the sketch along the Z-axis for 96 millimeters, using the sketch defined on the XY plane with eight entities consisting of arcs and lines arranged within a 37 by 93 millimeter bounding box.
