Create a base shape by extruding a sketch along the Z-axis for 2 millimeters. Then, create a symmetrical revolved feature around the central axis using another identical sketch on the XY plane. Ensure both sketches have five lines forming a similar layout within the same bounding box dimensions.
