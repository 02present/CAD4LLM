Create a base body by extruding a circle sketch on the XY plane 38 mm blind, then chamfer the edges. Next, create a smaller circular sketch on the same plane and extrude it 5 mm blind to form a second feature.
