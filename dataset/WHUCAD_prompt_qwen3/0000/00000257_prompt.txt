Create a base shape by extruding a sketch along the XY plane with a depth of 64 mm, then chamfer the edges. Next, create a second extrusion using a slightly smaller sketch on the same plane with a depth of 80 mm, forming a stacked structure.
