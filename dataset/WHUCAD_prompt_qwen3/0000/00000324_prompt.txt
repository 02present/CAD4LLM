Create a base shape by sketching an arc and lines on the XY plane with a bounding box from 0 to 45 in X and -16 to 95 in Y, then extrude it 32 mm to form a new body. Add a chamfer to the edge, then sketch another set of lines and arcs on the same plane but with a smaller bounding box, and extrude this second sketch 125 mm to create a second separate body.
