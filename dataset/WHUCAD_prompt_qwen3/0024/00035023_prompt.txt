Create a base shape by sketching a series of lines on the XY plane with dimensions 84mm by 41mm, then extrude it 85mm to form a solid body. Add a chamfer to one edge, and create another sketch slightly offset below the first, then extrude it 11mm to form a second separate feature.
