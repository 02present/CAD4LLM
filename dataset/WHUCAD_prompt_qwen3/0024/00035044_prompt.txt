Create a base shape by sketching four lines on the XY plane with a width of 4mm and height of 95mm, then revolve it around an axis to form a symmetrical 3D object. Next, extrude five separate sketches along the Z-axis with varying heights of 8mm and one of 64mm to build up the main structure. Each extrusion should create a new body, forming the core components of the model.
