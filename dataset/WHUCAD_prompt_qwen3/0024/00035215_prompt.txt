Create a base shape by extruding a sketch on the XY plane with six lines, then repeat the same sketch and extrusion process to form additional features. Add chamfers to two specific edges and create larger extrusions using different sketch dimensions and greater distances.
