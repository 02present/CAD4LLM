Create a base shape by extruding a sketch on the XY plane with a 41 mm depth, then add a chamfer to the edge. Next, create a second sketch on the same plane but larger, and extrude it 18 mm to form a separate feature.
