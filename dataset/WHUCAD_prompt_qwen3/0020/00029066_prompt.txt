Create a base body by extruding a sketch on the XY plane with a 55x95mm bounding box 64mm deep. Then, chamfer three edges. On the same plane, create a smaller sketch with a 45x95mm bounding box and extrude it 38mm deep, applying chamfers to the resulting features.
