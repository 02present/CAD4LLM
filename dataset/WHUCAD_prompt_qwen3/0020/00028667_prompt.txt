Create a base shape by extruding a sketch along the Z-axis for 24 mm, then create a second identical extrusion on top of the first. Finally, chamfer two corners of the top face with a 24 mm edge length.
