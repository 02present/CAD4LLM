Create a base shape by extruding a sketch on the XY plane with a 22 mm depth, then add a chamfer to the edge. Next, create a second sketch on the XY plane and extrude it 7 mm deep to form a separate feature.
