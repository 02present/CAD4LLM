Create a base shape by extruding a sketch consisting of four lines on the XY plane to a depth of 80 mm. Then, chamfer the edges. Next, create a circular sketch on the XY plane centered around the origin with a diameter of 96 mm and extrude it to a depth of 96 mm as a separate body.
