Create a base body by extruding a circle sketch on the XY plane 69 mm upwards, then chamfer the edges. Add a second circular sketch on the XY plane within a smaller bounding box and extrude it 34 mm upwards to form a separate feature.
