Create a base shape by extruding a sketch on the XY plane with a 27 mm depth, then add a chamfer to the edge. Next, create a second sketch slightly larger than the first and extrude it to form a second body, also 27 mm deep.
