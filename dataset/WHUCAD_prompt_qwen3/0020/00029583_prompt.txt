Create a base shape by extruding a sketch of five lines on the XY plane to a depth of 32 mm. Then, chamfer the edges. Next, create a circular sketch centered on the XY plane and extrude it to a depth of 64 mm to form a separate feature.
