Create a base shape by extruding a sketch of four lines on the XY plane to a depth of 64 mm. Then, chamfer the edges. On the same plane, sketch a circle and extrude it to a depth of 32 mm to form a separate feature.
