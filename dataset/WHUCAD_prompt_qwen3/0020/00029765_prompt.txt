Create a base shape by extruding a sketch consisting of four lines on the XY plane, then chamfer the edges. Next, create a second extrusion using a circular sketch on the XY plane, positioned lower and narrower than the first, with a slightly shorter depth.
