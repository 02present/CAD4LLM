Create a base shape by extruding a sketch consisting of four lines on the XY plane with a bounding box of 76x95 mm, then chamfer the edges. Next, create a second extrusion using a new sketch with three lines on the same plane but smaller dimensions, extruding it 64 mm to form a separate feature.
