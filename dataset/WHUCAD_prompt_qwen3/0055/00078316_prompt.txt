Create a base shape by sketching a circle on the XY plane with a bounding box of 28x15 to 80x67, then extrude it 96 mm to form a cylinder. Next, sketch four lines on the XY plane within a 95x87 bounding box and extrude them 96 mm to create a separate block. Finally, chamfer the edges where the two features meet.
