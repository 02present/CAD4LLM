Create a base shape by extruding a sketch on the XY plane with a 95x48mm bounding box 96mm upwards. Repeat this process with another identical sketch to form a second body, then chamfer the edges. Add additional features by extruding smaller sketches at different heights and sizes to complete the model.
