Create a base shape by extruding a sketch on the XY plane with four lines, then add three more similar extrusions using different sketches, each with a 39 mm depth. Apply chamfers to two specific edges and create additional features with varying dimensions and orientations.
