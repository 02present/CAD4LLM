Create a base shape by extruding a sketch on the XY plane with a 95mm by 54mm bounding box, then chamfer the edges. Next, create a second extrusion using a rotated sketch on the XY plane with a 54mm by 95mm bounding box and extrude it 126mm.
