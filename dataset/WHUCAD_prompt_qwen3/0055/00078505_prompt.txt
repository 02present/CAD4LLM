Create a base shape by extruding a sketch consisting of four lines on the XY plane, with a bounding box from 0,0 to 95,67, then extrude it 88 mm. Next, create a circular sketch on the XY plane centered at 0,-48 with a diameter of 96 mm and extrude it 32 mm to form a separate feature.
