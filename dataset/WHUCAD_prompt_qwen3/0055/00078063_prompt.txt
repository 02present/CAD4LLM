Create a base shape by extruding a sketch along the XY plane with a 1 mm depth, then chamfer the edges. Repeat this process twice more using different sketches oriented along the same plane but with varying dimensions to form a series of connected features.
