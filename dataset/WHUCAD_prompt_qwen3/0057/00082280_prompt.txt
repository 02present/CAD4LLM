Create a base shape by extruding a sketch on the XY plane with four lines, then add another extrusion using a second sketch with six lines on the same plane, and finally apply a chamfer to the edges. Add a third extrusion using a larger sketch on the XY plane with four lines, extending it slightly shorter than the previous ones.
