Create a base feature by extruding a sketch with six entities, including arcs and lines, on the XY plane for 1 mm. Then, create a second extruded feature using another sketch with similar entities on the same plane, but extrude it 2 mm to form a separate body.
