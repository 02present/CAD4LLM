Create a base body by extruding a sketch on the XY plane with a 96 mm depth. Then, add two chamfers to the edges. On the same plane, create two more extrusions with 31 mm depths using separate sketches.
