Create a base shape by extruding a sketch on the XY plane with a 95x82 mm bounding box, then chamfer the edges. Next, create a second extrusion using a smaller sketch on the XY plane with a 64x95 mm bounding box and extrude it 70 mm.
