Create a base shape by extruding a sketch on the XY plane with a 95x63 mm bounding box, then add two chamfers to the edges. Next, create a second extrusion from a slightly smaller sketch on the same plane, extending 34 mm, and apply a final chamfer to complete the design.
