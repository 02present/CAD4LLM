Create a base shape by extruding a sketch along the XY plane with a depth of 38 mm. Repeat this process using another sketch with the same dimensions but a different position, and extrude it to the same depth. Add a chamfer to one of the edges, then create a smaller extrusion from a new sketch with half the depth of the previous ones. Finally, add a circular feature and extrude it to a shallower depth of 10 mm.
