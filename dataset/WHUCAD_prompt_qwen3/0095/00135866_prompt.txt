Create a base body by extruding a circle sketch along the Z-axis for 96 millimeters, then create a second body by extruding another circle sketch along the Z-axis for 79 millimeters, positioning it so that the two extrusions form a stacked assembly.
