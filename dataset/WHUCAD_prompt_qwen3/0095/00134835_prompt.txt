Create a base shape by sketching eight lines on the XY plane within a 95x80 mm bounding box, then extrude it 42 mm to form a solid body. Add a chamfer to the edge, and create a circular sketch in the same plane but centered with a 96x96 mm bounding box, then extrude it 67 mm to form a second separate body.
