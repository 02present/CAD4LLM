Create a base body by extruding a sketch along the Z-axis for 96 mm, then add a chamfer to the edge. Repeat this process using two additional sketches, each extruded 96 mm along the Z-axis, positioned on different planes to form a stacked structure.
