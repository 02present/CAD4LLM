Create a base shape by sketching four lines on the XY plane forming a square with a 95mm side length, then revolve it around the Z-axis to form a symmetrical 3D object. Next, extrude a circular sketch with a 96mm diameter and 48mm radius downward by 54mm to create a separate body. Repeat this process using smaller sketches with similar dimensions but shorter extrusion distances of 18mm to build additional features.
