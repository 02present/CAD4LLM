Create a base shape by extruding a sketch on the XY plane with a 65mm by 95mm bounding box 64mm deep. Then, create a second extrusion using a slightly smaller sketch on the same plane, 63mm by 95mm, and extrude it 70mm deep to form a separate body.
