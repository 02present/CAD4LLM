Create a base shape by sketching four lines on the XY plane forming a square with a 95mm bounding box and extruding it 7mm. Then, chamfer the edges. Next, sketch a small circle near the top-right corner of the base and extrude it 7mm to form a separate feature.
