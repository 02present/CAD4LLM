Create a base shape by extruding a sketch consisting of four lines on the XY plane, then chamfer the edges. Next, create a second extrusion using an XY sketch with eight entities, including arcs and lines, to form the final part. Both extrusions should be 96 millimeters in height.
