Create a base shape by extruding a sketch on the XY plane with a 80 mm depth, then add two chamfers to the edges. Next, create a second sketch on the XY plane with slightly larger dimensions and extrude it 12 mm to form a separate feature.
