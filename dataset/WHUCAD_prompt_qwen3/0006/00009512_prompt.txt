Create a base shape by extruding a sketch with six lines on the XY plane, then add another extrusion using a new sketch with seven lines, followed by additional extrusions using subsequent sketches, each with different dimensions and positions, to form a complex 3D structure.
