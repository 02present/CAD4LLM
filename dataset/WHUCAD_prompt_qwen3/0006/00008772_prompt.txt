Create a base body by extruding a circle sketch along the Z-axis for 96 millimeters, then create a second body by extruding another circle sketch along the Z-axis for 32 millimeters. Both sketches should be on the XY plane with a diameter of 96 millimeters and centered within a 96 by 96 millimeter bounding box.
