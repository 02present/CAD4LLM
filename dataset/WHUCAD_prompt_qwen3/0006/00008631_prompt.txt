Create a base shape by extruding a sketch on the XY plane with four lines, then add another extrusion using a different sketch with five lines, followed by a chamfer on the edges. Continue by creating more extruded features using additional sketches, each with a consistent extrusion depth of 2 millimeters.
