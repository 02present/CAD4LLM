Create a base part by extruding a circle sketch 32 mm to form a cylinder. Then chamfer the edge. Next, sketch four lines on the XY plane to form a rectangle and extrude it 48 mm to create a separate block.
