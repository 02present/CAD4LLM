Create a base shape by extruding a sketch on the XY plane with a 59 mm depth, then add a chamfer to the edge. Next, create a second sketch on the same plane but slightly larger and extrude it 24 mm to form a separate body.
