Create a base shape by extruding a sketch on the XY plane with six lines, then add similar extruded features using different sketches and distances. Finally, chamfer one edge and create a circular hole by extruding a circle along the vertical axis.
