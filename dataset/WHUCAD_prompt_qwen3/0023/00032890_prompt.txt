Create a base shape by extruding a sketch on the XY plane with four lines, then extrude another sketch on the same plane with four lines to form a second body. Add a chamfer to the edges and create a cylindrical feature by extruding a circle sketch to complete the design.
