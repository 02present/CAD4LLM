Create a base body by extruding a circle sketch along the Z-axis for 96 millimeters, then create a second separate body by extruding another circle sketch along the Z-axis for 50 millimeters. Both sketches should be on the XY plane with the same bounding box dimensions.
