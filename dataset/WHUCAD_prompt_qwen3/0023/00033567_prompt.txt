Create a base shape by sketching a circle on the XY plane with a bounding box of 42x22 to 54x34, then extrude it 24 mm to form a cylinder. Next, sketch nine lines on the XY plane within a 95x41 bounding box and extrude them 70 mm to create a larger block. Finally, chamfer the edges where the two features meet.
