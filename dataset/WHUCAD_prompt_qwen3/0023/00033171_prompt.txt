Create a base body by extruding a circle sketch along the Z-axis for 96 millimeters. Then, chamfer the edge of the base body. Next, create a second circular sketch on the same plane and extrude it 51 millimeters to form a smaller protrusion.
