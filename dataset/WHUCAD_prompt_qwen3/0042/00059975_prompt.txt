Create a base shape by sketching a series of lines and an arc on the XY plane, then extrude it 10 millimeters to form a solid body. Add a chamfer to one edge, and create a circular sketch on the same plane but centered lower, then extrude it 2 millimeters to form a second separate feature.
