Create a base shape by extruding a sketch on the XY plane with a 96 mm depth, then add two chamfers to the edges. Next, create a second sketch on the XY plane with a smaller bounding box and extrude it 53 mm deep to form a separate feature.
