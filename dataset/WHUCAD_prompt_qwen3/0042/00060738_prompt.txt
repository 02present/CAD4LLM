Create a base shape by sketching four lines on the XY plane forming a rectangle with dimensions 95mm by 62mm, then extrude it 96mm to form a solid body. Add two chamfers to the edges, and create a circular sketch in the XY plane centered at the origin with a diameter of 96mm, then extrude it 5mm to form a separate feature.
