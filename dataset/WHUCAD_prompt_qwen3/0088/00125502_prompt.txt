Create a base shape by sketching two arcs on the XY plane with a bounding box from [0.0, -87.0, 4.0, 0.0], then extrude this sketch 32 mm to form a new body. Repeat this process twice more using similar arc sketches on the same plane but with slightly different bounding boxes, and extrude each 32 mm to create three separate features. Finally, chamfer the edges where the features meet.
