Create a base body by extruding a sketch along the XY plane with a depth of 96 mm, then add two chamfers to the edges. On the same plane, create a smaller sketch and extrude it 42 mm to form a secondary feature.
