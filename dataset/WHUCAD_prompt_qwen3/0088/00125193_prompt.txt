Create a base shape by extruding a sketch on the XY plane with a 95x25mm bounding box 105mm upwards. Then chamfer the edges. Add another sketch on the same plane but with a larger 95x88mm bounding box and extrude it 105mm upwards as a separate body.
