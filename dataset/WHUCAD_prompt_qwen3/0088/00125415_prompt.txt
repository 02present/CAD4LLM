Create a base shape by extruding a sketch with four lines on the XY plane to a depth of 112 mm. Repeat this process using identical sketches to form multiple parallel features, then chamfer the edges at each connection point. Add additional smaller extrusions with varying depths to complete the detailed structure.
