Create a base shape by extruding a sketch on the XY plane with a 95x67mm bounding box, then add a chamfer. Next, create a second extrusion using a smaller sketch on the same plane with a 95x60mm bounding box, extruding 60mm to form a separate feature.
