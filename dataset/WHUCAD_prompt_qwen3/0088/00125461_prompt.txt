Create a base shape by extruding a sketch consisting of four lines on the XY plane with a bounding box of 95mm by 47mm, then chamfer the edges. Next, create a second extrusion using a new sketch with six lines on the same plane but a smaller bounding box of 95mm by 12mm, extruding it 68mm to form a separate body.
