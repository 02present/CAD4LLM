Create a base body by extruding a sketch on the XY plane with a 24mm by 95mm bounding box, then chamfer the edges. Add three more extruded features using separate sketches on the XY plane, each with a 85mm by 9mm bounding box, all extruded 9mm in height.
