Create a base body by extruding a sketch with 12 entities on the XY plane, then create a second body by extruding another sketch with 8 entities on the same plane, both extrusions extending 96 millimeters.
