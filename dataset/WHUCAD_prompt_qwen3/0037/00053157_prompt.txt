Create a symmetrical part by first sketching a series of lines on the XY plane to form a base profile, then revolving it around an axis to create a 3D shape. Next, chamfer the edges of the revolved feature. On the same plane, sketch another set of lines to define a second profile and extrude it 1 millimeter to form a separate body. Ensure all sketches are aligned for symmetry.
