Create a base shape by sketching five lines and an arc on the XY plane within a 94x84 mm bounding box, then revolve it around the Z-axis to form a symmetrical 3D object. Next, create a second identical sketch on the same plane and extrude it 32 mm to form a separate body, ensuring both features are distinct and properly oriented.
