Create a base body by extruding a sketch with nine lines on the XY plane, 64 mm deep. Then, create two additional bodies by extruding separate sketches on the XY plane, 32 mm deep each, positioned to form a stacked structure.
