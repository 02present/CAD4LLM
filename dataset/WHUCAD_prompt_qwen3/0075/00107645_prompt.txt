Create a new body by extruding the sketch along the Z-axis for a distance of 101 millimeters, using the sketch defined on the XY plane with lines forming a complex shape within the specified bounding box.
