Create a base body by extruding a sketch along the Z-axis for 96 millimeters. Add two chamfers to the edges of the base body. Then, create a smaller protrusion by extruding another sketch 4 millimeters upwards and a third larger extrusion matching the first height using the third sketch.
