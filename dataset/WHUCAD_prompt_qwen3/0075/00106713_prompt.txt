Create a base shape by sketching four lines on the XY plane with a bounding box from 0 to 84 in X and -2 to 90 in Y, then extrude it 96 mm to form a new body. Add a chamfer to the edge, and create a second smaller feature by sketching another set of four lines on the XY plane with a slightly adjusted bounding box, then extrude it 3 mm to form a separate body.
