Create a base shape by extruding a circle from the first sketch 81 millimeters. Then, chamfer the edges. Add a second circular sketch on the XY plane and extrude it 40 millimeters to form a separate feature.
