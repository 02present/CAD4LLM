Create a base body by extruding a sketch on the XY plane with a 76x95mm bounding box 96mm upwards. Then chamfer the edges. Add two more extrusions, each 48mm high, based on separate sketches with a 95x58mm bounding box.
