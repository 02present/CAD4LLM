Create a base shape by extruding a sketch on the XY plane with four lines, then add another extruded feature using a smaller sketch, followed by two chamfers on the edges. Repeat the extrusion process with varying distances and sketch sizes to build up the final form.
