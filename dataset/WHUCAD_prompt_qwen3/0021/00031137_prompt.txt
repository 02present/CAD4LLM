Create a base shape by extruding a sketch on the XY plane with an 83x88 mm bounding box, then add three chamfers. Next, create a second extrusion using a new sketch on the XY plane with a smaller bounding box, followed by two more chamfers. Finally, extrude a third sketch on the XY plane with a narrower bounding box to form the final part.
