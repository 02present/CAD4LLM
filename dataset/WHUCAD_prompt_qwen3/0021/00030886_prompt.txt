Create a base shape by extruding a sketch on the XY plane with six lines, then chamfer the edges. Add a second extrusion using a smaller sketch on the XY plane with four lines, extending 18 mm to form a separate feature.
