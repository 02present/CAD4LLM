Create a base shape by extruding a sketch on the XY plane with dimensions 63mm by 95mm, then add two chamfers to the edges. Next, create a smaller sketch on the same plane with dimensions 95mm by 56mm and extrude it to form a second body. Finally, add several small circular sketches around the model to define additional features.
