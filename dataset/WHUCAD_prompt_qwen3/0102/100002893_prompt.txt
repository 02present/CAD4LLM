Create a base shape by extruding a series of lines on the XY plane to form a flat profile, then build up the main body by extruding additional sketches at varying heights. Add circular features along the length and include a revolved element to complete the design.
