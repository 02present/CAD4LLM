Create a base shape by sketching a symmetrical pattern of lines and an arc on the XY plane, then revolve it around the Z-axis to form a 3D object. Next, extrude a smaller sketch with four lines and an arc along the Z-axis for a depth of 3 millimeters. Finally, add a circular sketch centered on the XY plane to define a hole through the center of the part.
