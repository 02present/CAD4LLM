Create a base shape by extruding a sketch with four lines on the XY plane, then add another extrusion using a smaller sketch to form a secondary feature. Add two circular sketches near the edges and create a final sketch with four lines for additional detailing.
