Create a base shape by sketching a complex profile on the XY plane with 12 lines and an arc, then extrude it 6 mm to form a solid body. Next, sketch a simpler profile on the same plane with four lines and extrude it 6 mm to create another separate body. Finally, add two circular sketches on the XY plane, one centered and one offset, to define additional features.
