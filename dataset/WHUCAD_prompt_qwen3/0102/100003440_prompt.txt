Create a base shape by extruding a sketch on the XY plane with 12 lines, then chamfer the edges. Add four more extrusions using separate sketches on the XY plane, each with different dimensions and positions, to form the final assembly.
