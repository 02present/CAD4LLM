Create a base shape by sketching eight connected line and arc segments on the XY plane within the specified bounding box, then extrude this sketch 30 millimeters to form a new body. Additionally, sketch a circle on the same plane within a smaller bounding box and extrude it as well.
