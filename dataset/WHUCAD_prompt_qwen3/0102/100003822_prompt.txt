Create a base feature by extruding a circle from the first sketch along the Z-axis for 96 millimeters. Then, add a second circle on top of the base and extrude it as well. Finally, draw four lines on the XY plane to form a rectangular shape and use them as a reference for the final extrusion.
