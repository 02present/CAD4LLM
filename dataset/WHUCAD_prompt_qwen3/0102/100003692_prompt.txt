Create a base shape by extruding a sketch consisting of three lines on the XY plane, then add a second extrusion using a circular sketch in the same plane but centered and smaller, followed by a third extrusion based on another set of four lines, all extending 32 millimeters deep.
