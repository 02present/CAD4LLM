Create a base shape by extruding a sketch with four lines on the XY plane, then add a revolved feature using a new sketch. Add several additional sketches on the same plane to define smaller, aligned features along the length of the part.
