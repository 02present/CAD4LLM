Create a base shape by extruding a sketch on the XY plane with a 40 mm depth, then add two chamfers to the edges. Next, create another sketch slightly smaller than the first and extrude it with a 96 mm depth to form the second body.
