Create a base shape by extruding a sketch consisting of four lines on the XY plane with a width of 3mm and height of 95mm, then chamfer the edges. Next, create a second extrusion using a smaller sketch of four lines on the same plane but with a width of 2mm, extruding it 127mm to form a separate body.
