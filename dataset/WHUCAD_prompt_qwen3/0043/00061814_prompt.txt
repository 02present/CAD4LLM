Create a base shape by extruding a sketch consisting of five lines on the XY plane with a width of 5mm and height of 95mm, then chamfer two edges. Next, create a second extrusion using a smaller sketch with four lines on the same plane, extending 127mm, while keeping the first extrusion as a separate body.
