Create a base body by extruding a sketch on the XY plane with a 95x65mm bounding box, then chamfer the edges. Add a second extruded feature using a smaller sketch on the same plane with a 95x15mm bounding box, and chamfer its edges as well.
