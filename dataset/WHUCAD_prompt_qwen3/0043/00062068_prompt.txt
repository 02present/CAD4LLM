Create a base shape by extruding a square sketch with four lines on the XY plane to a depth of 96 mm. Then, chamfer the edges. On the same plane, create three circular sketches, each centered and aligned vertically, and extrude each to 96 mm, applying a chamfer to each resulting feature as well.
