Create a base shape by extruding a sketch on the XY plane with four lines, then add another extrusion using a smaller sketch, followed by a chamfer on the edge. Continue by creating additional extrusions from subsequent sketches, each time adjusting the sketch dimensions and positioning to form the final detailed structure.
