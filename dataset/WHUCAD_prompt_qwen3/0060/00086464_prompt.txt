Create a base shape by extruding a circle with a diameter of 96 mm along the Z-axis. Then, add three identical rectangular protrusions by extruding three lines each 84 mm tall, positioned symmetrically along the length of the base. Repeat this pattern on both sides and add a final larger rectangular section at the top using six lines to form a wider structure.
