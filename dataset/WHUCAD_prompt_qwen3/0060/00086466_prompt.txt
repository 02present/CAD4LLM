Create a base shape by extruding a sketch along the XY plane for 58 millimeters, then chamfer the edges. Next, create a second extrusion using a slightly larger sketch on the same plane, extending it 82 millimeters to form the final structure.
