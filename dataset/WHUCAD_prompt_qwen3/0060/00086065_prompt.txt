Create a base shape by extruding a sketch of four lines on the XY plane with a 95x90mm bounding box 33mm upwards. Then chamfer the edges. Add a smaller extrusion of four lines on the same plane but with a 95x29mm bounding box, extending 16mm upwards to form a separate feature.
