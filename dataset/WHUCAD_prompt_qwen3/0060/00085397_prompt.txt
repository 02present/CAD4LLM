Create a base shape by extruding a sketch on the XY plane with a 62x95mm bounding box, then chamfer the edges. Add a second extruded feature based on a smaller sketch with a 48x95mm bounding box, both extruded 77mm in the same direction.
