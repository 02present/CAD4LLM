Create a base body by extruding a circle with a 16 mm diameter along the Z-axis. Then, create another body by extruding a larger circle with a 47 mm diameter along the same axis, positioning it so that it extends beyond the first body.
