Create a base shape by extruding a sketch on the XY plane with four lines, then repeat the same sketch and extrusion process to form a second body. Add chamfers to the edges of both bodies, and create a smaller extrusion from a new sketch on the XY plane with ten lines, positioning it at the top.
