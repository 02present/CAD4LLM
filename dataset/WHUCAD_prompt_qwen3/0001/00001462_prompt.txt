Create a base shape by extruding a sketch on the XY plane with a 96 mm height, then add a chamfer to the edge. Next, create two more extrusions using separate sketches on the XY plane with heights of 89 mm each, ensuring all features are properly aligned and connected.
