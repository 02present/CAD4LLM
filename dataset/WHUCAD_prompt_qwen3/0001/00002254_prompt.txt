Create a base shape by sketching four lines on the XY plane with dimensions 69mm by 95mm, then extrude it 49mm to form a solid body. Repeat the sketch and extrusion process to create a second identical body, then chamfer the edges. Finally, sketch another set of lines on the XY plane shifted down and revolve it around an axis to complete the design.
