Create a base shape by extruding a series of sketched lines on the XY plane, with each extrusion having a specified depth. Add additional features by repeating the process with different sketch dimensions and extrusion distances, and finish by chamfering one of the edges.
