Create a base shape by sketching a circle on the XY plane with a bounding box from 32 to 64 in both directions and extruding it 21 millimeters. Then, chamfer the edge. Next, sketch another circle on the XY plane within a smaller bounding box and extrude it 7 millimeters to form a secondary feature.
