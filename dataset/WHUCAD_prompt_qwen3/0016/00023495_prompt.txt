Create a base shape by extruding a sketch on the XY plane with a 95mm width and 53mm height, then chamfer the edges. Next, create a second extrusion using a smaller sketch on the same plane with a 95mm width and 48mm height, extending it 126mm to form a separate body.
