Create a base shape by extruding a sketch on the XY plane with three lines, then chamfer the edges. Repeat this process four times using different sketches positioned at varying Y-offsets, each extruded 7 millimeters to form a series of stacked features.
