Create a base shape by extruding a sketch on the XY plane with four lines, then repeat the process using different sketches on the same plane to form multiple extruded features. Add a chamfer to one of the edges and create another extrusion from a new sketch oriented differently on the XY plane.
