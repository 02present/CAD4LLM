Create a base body by extruding a circle with a 96 mm diameter along the Z-axis. Then, chamfer the edge of the base body. Next, create a second circular sketch centered on the base body with a smaller diameter and extrude it 119 mm upwards to form a separate feature.
