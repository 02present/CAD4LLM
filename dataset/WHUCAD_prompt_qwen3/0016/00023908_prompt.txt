Create a base shape by extruding a sketch on the XY plane with a 51 mm depth, then add a chamfer to the edge. Next, create a second sketch slightly larger than the first and extrude it 6 mm to form a separate feature.
