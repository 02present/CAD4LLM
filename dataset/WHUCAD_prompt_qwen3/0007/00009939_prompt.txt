Create a base shape by extruding a circle with a 15mm depth. Then chamfer the edges. Add several smaller extrusions on top of the base, each with a 1mm depth, arranged in a grid pattern across the surface.
