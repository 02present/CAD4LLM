Create a base shape by extruding a sketch on the XY plane with a 95x48mm bounding box, then repeat the same sketch and extrusion process to form a second body. Add chamfers to two corners of the final assembly.
