Create a base shape by extruding a sketch consisting of four lines on the XY plane with a width of 2mm and height of 95mm, then chamfer the edges. Next, create a second extrusion using a larger sketch on the same plane but with a width of 12mm, extruding it 116mm to form a separate body.
