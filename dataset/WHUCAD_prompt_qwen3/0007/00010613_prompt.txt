Create a base shape by extruding a sketch on the XY plane with four lines, repeating this process five times with each subsequent sketch positioned similarly, then chamfer the edges of the final structure.
