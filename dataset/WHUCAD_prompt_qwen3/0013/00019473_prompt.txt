Create a base shape by extruding a sketch on the XY plane with a 23 mm depth, then add a chamfer to the edge. Next, create a second sketch slightly smaller and extrude it 32 mm to form a secondary feature.
