Create a base shape by extruding a square sketch with 4 lines on the XY plane to a depth of 64 mm. Then, chamfer each corner of the resulting face. Repeat this process to create multiple identical extruded blocks, each with their own chamfers, and add additional larger extrusions on top using the same square sketches but with a greater depth of 126 mm.
