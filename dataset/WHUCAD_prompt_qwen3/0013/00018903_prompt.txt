Create a base shape by extruding a sketch on the XY plane with a 48x95mm bounding box, then add two chamfers to the edges. Next, create a second extrusion using a larger sketch on the same plane with a 71x95mm bounding box, also extruded 72mm.
