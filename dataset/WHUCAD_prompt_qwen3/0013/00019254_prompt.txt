Create a base by extruding a circle sketch 3mm to form a cylinder. Then chamfer the edges. Next, create a larger square sketch and extrude it 11mm to form a block. Finally, repeat the square sketch and extrude it another 11mm to add a second block.
