Create a base shape by extruding a sketch along the Z-axis for 25 millimeters. Then, create a second identical sketch on the same plane and add a chamfer to one of the edges. Finally, draw a circle on the same plane and use it as a reference for further features.
