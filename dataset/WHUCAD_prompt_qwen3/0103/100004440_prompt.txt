Create a base shape by sketching 12 lines on the XY plane with a bounding box from [0, -32] to [95, 13], then extrude it 19 mm to form a solid body. Next, sketch 8 elements including lines and arcs on the same plane with a smaller bounding box, and extrude that 16 mm to create another separate body. Also, add two circles on the XY plane, one with a bounding box from [0, -20] to [40, 20] and another from [55, -90] to [95, -50], and extrude each to form additional features.
