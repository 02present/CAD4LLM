Create a base shape by sketching ten lines on the XY plane forming a wide rectangular layout, then revolve it around an axis to form a symmetrical 3D object. Add a second revolved feature using a smaller arc and line sketch, and extrude two separate sketches into new bodies with specified depths. Finally, add circular features at specific positions along the length of the model.
