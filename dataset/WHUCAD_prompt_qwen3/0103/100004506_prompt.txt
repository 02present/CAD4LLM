Create a base shape by sketching 15 lines on the XY plane within a 95x68mm bounding box, then revolve it around the Z-axis. Add a smaller sketch with arcs and lines on the XY plane to form a secondary feature, and repeat this process to create additional detailed sections across the design.
