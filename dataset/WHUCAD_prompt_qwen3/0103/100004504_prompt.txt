Create a base body by extruding a circle sketch along the Z-axis for 5 millimeters. Then, create another identical extrusion using a second circle sketch. Finally, extrude a set of five lines arranged in a rectangular pattern across the XY plane for 5 millimeters to form a separate feature.
