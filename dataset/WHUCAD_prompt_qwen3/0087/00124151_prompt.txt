Create a base shape by extruding a sketch on the XY plane with a 95x61mm bounding box, then add a chamfer. Next, create a second extruded feature using a smaller sketch on the same plane with a 95x55mm bounding box, extruding it 68mm.
