Create a base shape by sketching five lines on the XY plane within a 95mm wide and 96mm tall bounding box, then extrude it 21mm to form the main body. Add two chamfers to the edges, then create a second sketch with four lines on the XY plane forming a square, extrude it 62mm to form a separate feature, and apply a final chamfer to its corners.
