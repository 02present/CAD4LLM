Create a base body by extruding a sketch consisting of three lines on the XY plane with a bounding box of 95mm by 49mm, then create a second identical body using the same sketch and extrusion distance. Add a third extruded feature using a new sketch with four lines on the XY plane, but with a smaller bounding box of 51mm by 95mm and a shorter extrusion distance.
