Create a base shape by extruding a sketch on the XY plane with a 95x57mm bounding box, then extrude another sketch on the same plane but with a larger 95x95mm bounding box to form a second body. Finally, chamfer the edges of the resulting structure.
