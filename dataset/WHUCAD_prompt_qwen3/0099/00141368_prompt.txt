Create a base body by extruding a circle sketch along the Z-axis for 96 millimeters. Then, create a second extruded feature by extruding another circle sketch for 80 millimeters. Finally, chamfer the edges where the two features intersect.
