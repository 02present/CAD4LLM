Create a base shape by extruding a sketch along the Z-axis for 49 millimeters. Then, chamfer the edges of the resulting body. Next, create a second sketch slightly smaller than the first and extrude it 20 millimeters to form a secondary feature.
