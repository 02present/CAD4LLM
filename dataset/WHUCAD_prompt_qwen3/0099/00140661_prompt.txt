Create a base shape by extruding a sketch on the XY plane with four lines, then add another extruded feature using a new sketch with six lines. Apply chamfers to four edges, and repeat the process with additional sketches and extrusions to build up the final model. Each extrusion should be 50 mm deep and create a new body.
