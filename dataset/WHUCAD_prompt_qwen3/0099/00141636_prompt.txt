Create a base shape by extruding a sketch on the XY plane with four lines, then add another extrusion using a larger sketch to form a taller section. Continue by extruding a longer sketch to create the main body of the part, and finally add a chamfer to one of the edges. Include smaller extrusions for details, using sketches with lines and an arc to define specific features.
