Create a base shape by sketching ten lines on the XY plane with a bounding box from 0 to 95 in the X direction and -74 to 24 in the Y direction, then extrude it 96 millimeters to form a solid body. Repeat the sketch process slightly adjusted in size and position, then extrude it 1 millimeter to create a second, smaller feature.
