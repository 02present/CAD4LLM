Create a base shape by extruding a sketch on the XY plane with a 20 mm height, then add a chamfer to the edge. Next, create two more extrusions using separate sketches on the same plane, each with a height of 112 mm, forming a stacked structure. Ensure all sketches are defined with four lines within their respective bounding boxes.
