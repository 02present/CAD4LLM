Create a base shape by extruding a sketch of four lines on the XY plane to a depth of 48 mm. Then, create a circular sketch on the same plane but shifted down by 48 mm and extrude it to a depth of 21 mm to form a separate feature.
