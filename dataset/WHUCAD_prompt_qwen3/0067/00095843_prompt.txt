Create a base shape by extruding a square sketch along the Z-axis for 48 mm. Then, create a larger circular protrusion by extruding a circle on the XY plane 80 mm upwards. Finally, add a chamfer to one of the edges where the two features meet.
