Create a base shape by extruding a sketch on the XY plane with a 40 mm depth, then add a chamfer to the edge. On the same plane, create a second sketch slightly smaller and extrude it another 40 mm to form a secondary feature.
