Create a base feature by extruding a circle sketch 12 millimeters to form a solid body. Then, chamfer the edge of this extrusion. Next, create another extrusion using a second identical circle sketch, extending it 96 millimeters to form a separate solid body.
