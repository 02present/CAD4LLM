Create a base shape by sketching four connected arcs and lines on the XY plane, then extrude it 36 mm to form a solid body. Add a chamfer to one edge, and create a circular sketch on the XY plane centered at 32, 12 with a diameter matching the original sketch, then extrude it 24 mm to form a second separate body.
