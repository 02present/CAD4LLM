Create a base shape by extruding a square sketch on the XY plane 50 mm upwards. Repeat this process using three identical sketches positioned along the same plane to form a series of stacked blocks. Ensure each extrusion creates a separate body.
