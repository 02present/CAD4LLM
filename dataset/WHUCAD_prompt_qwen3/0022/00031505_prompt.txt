Create a base body by extruding a circle sketch on the XY plane 37 mm blind, then chamfer the edge. Add a second circular sketch on the XY plane and extrude it 25 mm blind to form a separate feature.
