Create a base shape by extruding a sketch consisting of six lines on the XY plane with a bounding box of 79x95 mm, then chamfer the edges. Next, create a second extrusion using a new sketch with three lines on the same plane but smaller dimensions, extruding it 96 mm to form a separate body.
