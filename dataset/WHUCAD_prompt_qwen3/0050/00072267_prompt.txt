Create a base shape by sketching a series of lines on the XY plane with a bounding box from 0 to 88 in X and -40 to 40 in Y, then extrude it 12 millimeters. Add two chamfers to the edges, then create a circular sketch on the same plane with a larger bounding box and extrude it 7 millimeters. Finally, add another linear sketch with four lines and extrude it 7 millimeters as well.
