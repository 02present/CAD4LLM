Create a base body by extruding a sketch along the XY plane with a 3 mm depth, then add two chamfers to the edges, each on separate faces, and repeat the process with another identical sketch and extrusion.
