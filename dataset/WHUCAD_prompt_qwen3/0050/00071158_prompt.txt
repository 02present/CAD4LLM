Create a base shape by sketching nine lines on the XY plane with a bounding box of 66mm by 95mm, then extrude it 2mm to form a solid. Add a chamfer to the edge, and create a second sketch with eleven elements including an arc on the same plane, then extrude that sketch 2mm to form a separate body.
