Create a base body by extruding a circle with a 14 mm diameter along the Z-axis. Then, create a second body by extruding a larger circle with a 41 mm diameter along the Z-axis, positioning it so that it extends beyond the first body.
