Create a base shape by extruding a sketch on the XY plane with a 95x95mm bounding box, then add a second extrusion using a smaller sketch on the same plane but with a 21x95mm bounding box, and finally chamfer the edges of the resulting structure.
