Create a base shape by sketching an 11-entity pattern on the XY plane with lines and arcs within a 95x95mm bounding box, then extrude it 96mm to form a solid body. Add a chamfer to the edge, followed by three more extrusions of 92mm each using different sketches on the XY plane, and finish with another chamfer on the final edge.
