Create a base shape by extruding a square sketch 72 mm in the Z-direction. Then, add three chamfers to the edges. Next, create a slightly larger square sketch and extrude it 80 mm. Add two more chamfers. Finally, create smaller rectangular sketches and extrude them 53 mm and 48 mm respectively, ending with a final chamfer on the last feature.
