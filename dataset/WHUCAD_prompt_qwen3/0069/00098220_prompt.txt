Create a base by extruding a circle sketch 13 mm to form a cylinder, then chamfer the edges. Next, sketch four lines on the XY plane and extrude them 36 mm to create a separate block, and apply three chamfers to its edges.
