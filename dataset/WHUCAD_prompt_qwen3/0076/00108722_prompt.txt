Create a base shape by extruding a sketch on the XY plane with a 95x59mm bounding box, then chamfer the edges. Next, create a second extrusion using a larger 95x95mm sketch on the same plane, extruding 70mm to form a separate body.
