Create a base shape by sketching eight lines on the XY plane with dimensions from 0 to 76 mm in width and 0 to 95 mm in height, then extrude it 96 mm to form a solid body. Chamfer the edges of the resulting body, then create a second sketch with four lines on the XY plane within a smaller bounding box and extrude it 32 mm to form a separate feature.
