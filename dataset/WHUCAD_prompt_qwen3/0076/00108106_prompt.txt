Create a base shape by extruding a sketch on the XY plane with a 95x79mm bounding box, then add a chamfer to the edges. Next, create a second extrusion using a lower sketch on the XY plane shifted down by 70mm, both extrusions extending 64mm in the same direction.
