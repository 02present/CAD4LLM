Create a base shape by extruding a sketch on the XY plane with a 16 mm depth, then add a chamfer to the edge. Next, create another sketch slightly larger than the first and extrude it 16 mm to form a second body.
