Create a base body by extruding a sketch on the XY plane with a 96 mm height. Chamfer the edges of the base body. Then, create two additional bodies by extruding two more sketches on the XY plane, each with a 96 mm height, and one final extrusion with a 64 mm height.
