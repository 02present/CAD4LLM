Create a base shape by extruding a sketch on the XY plane with a 95x89 mm bounding box, then chamfer the edges. Add a second extrusion using a slightly smaller sketch on the same plane, extending 121 mm to form a separate body.
