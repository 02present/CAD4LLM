Create a base shape by extruding a sketch with four lines on the XY plane, then chamfer the edges. Next, create a second extrusion using a smaller sketch with three lines on the same plane, and extrude it 64 mm to form a separate body.
