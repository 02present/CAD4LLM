Create a base body by extruding a sketch along the XY plane for 54 mm, then add a chamfer to the edge. On the same plane, create a second sketch with similar dimensions and extrude it 21 mm to form a separate feature.
