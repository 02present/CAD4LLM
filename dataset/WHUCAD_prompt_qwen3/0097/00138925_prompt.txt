Create a base shape by extruding a sketch on the XY plane with a 93x73mm bounding box, then chamfer the edges. Add a second extrusion using a slightly larger sketch on the same plane, extending 126mm to form a separate body.
