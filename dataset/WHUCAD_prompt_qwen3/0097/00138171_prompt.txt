Create a symmetrical base shape by extruding a series of sketched profiles along the Z-axis, each 27 mm deep, and apply a chamfer to one of the edges. Start with a circular sketch on the XY plane, then build up using multiple line and arc sketches to form the overall structure.
