Create a base by extruding a circle sketch 2mm to form a cylinder. Then chamfer the edges. Add several rectangular sketches on the XY plane and extrude each 13mm to create a series of stacked blocks. Ensure all sketches are properly aligned and positioned as indicated by their bounding boxes.
