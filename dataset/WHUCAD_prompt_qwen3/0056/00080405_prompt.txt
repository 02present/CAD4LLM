Create a base body by extruding a sketch of eight lines on the XY plane with a 95x79mm bounding box, then chamfer the edges. Next, create a second body by extruding a sketch of four lines on the same plane with a 95x32mm bounding box, and chamfer its edges as well.
