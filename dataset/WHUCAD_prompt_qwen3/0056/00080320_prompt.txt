Create a base feature by extruding a circle with a 5 mm depth, then chamfer the edge. Next, create a second circular sketch slightly smaller and lower than the first, and extrude it to 10 mm depth to form a separate body.
