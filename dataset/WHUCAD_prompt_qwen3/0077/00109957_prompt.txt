Create a base shape by extruding a sketch on the XY plane with six lines, then chamfer the edges. On the same plane, create a second sketch with three lines and extrude it to form a separate body, ensuring both extrusions are 96 millimeters in height.
