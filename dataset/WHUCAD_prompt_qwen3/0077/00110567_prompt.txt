Create a base shape by extruding a sketch on the XY plane with a 95x66 mm bounding box, then chamfer the edges. Next, create a second extrusion using a new sketch on the XY plane with a 72x95 mm bounding box and extrude it 23 mm.
