Create a base shape by extruding a sketch on the XY plane with a 72x95mm bounding box 96mm deep. Chamfer two edges of the resulting body. Then, create a second extrusion using a smaller sketch on the same plane with a 51x95mm bounding box, extruding it 126mm deep to form a separate feature.
