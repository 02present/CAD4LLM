Create a base shape by extruding a sketch on the XY plane with a 4x95mm bounding box, then chamfer the edges. Add a second extruded feature based on a slightly shifted sketch, maintaining the same dimensions and extrusion depth.
