Create a base body by extruding a circle sketch on the XY plane with a 96 mm depth. Then, chamfer the edge. Add a second circular sketch on the XY plane with a 60 mm extrusion to form a separate feature.
