Create a base body by extruding a circle with a 96 mm diameter along the Z-axis. Then, chamfer the edge of the base body. Next, create a smaller circular sketch on the XY plane and extrude it 11 mm to form a second feature.
