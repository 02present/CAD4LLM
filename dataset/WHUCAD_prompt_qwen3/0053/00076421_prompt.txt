Create a base shape by extruding a sketch on the XY plane with nine lines, then repeat the same sketch and extrusion process to form multiple stacked bodies. Add two chamfers to the edges and create a final extruded feature using a larger sketch on the XY plane.
