Create a base shape by extruding a sketch on the XY plane with a 95x57mm bounding box, then chamfer the edges. Add a second extrusion of a slightly larger sketch on the same plane, extending 38mm to form a separate feature.
