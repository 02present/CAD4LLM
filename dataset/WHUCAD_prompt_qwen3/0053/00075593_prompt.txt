Create a base body by extruding a sketch along the Z-axis for 96 mm, then add two chamfers to the edges, followed by two more extrusions based on identical sketches in the same plane, each also extruded 96 mm in height. Finally, create a third extrusion using a vertical sketch to form a separate feature.
