Create a base body by extruding a sketch on the XY plane with four lines, then chamfer the edges. Add two more extrusions using separate sketches on the XY plane, each with six and four lines respectively, all extending 40 mm. Ensure each extrusion creates a new body.
