Create a base body by extruding a circle sketch along the Z-axis for 96 millimeters. Then, chamfer the edge of the resulting body. Next, create a second circular sketch slightly larger than the first and extrude it along the Z-axis for 117 millimeters to form a separate body.
