Create a base shape by extruding a sketch with four lines on the XY plane to a depth of 8 mm. Add a chamfer to the edge, then create additional vertical and horizontal extrusions using similar sketches at different heights and depths, finishing with another chamfer on the top edge.
