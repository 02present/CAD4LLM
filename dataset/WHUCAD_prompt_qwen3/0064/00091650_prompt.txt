Create a base shape by extruding a sketch on the XY plane with five lines, then chamfer the edges. Add another extruded feature using a second sketch with four lines on the same plane, ensuring both extrusions are 64 millimeters deep.
