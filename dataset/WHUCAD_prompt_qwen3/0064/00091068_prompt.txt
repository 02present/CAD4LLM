Create a base shape by extruding a sketch on the XY plane with six lines, then add a second extrusion using a different sketch on the same plane with four lines, followed by two chamfers on the edges. Finally, create a third extrusion using another sketch on the XY plane with four lines, and apply a second chamfer to complete the design.
