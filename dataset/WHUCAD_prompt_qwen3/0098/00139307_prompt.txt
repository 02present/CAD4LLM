Create a base shape by extruding a sketch on the XY plane with a 92 mm height. Then, chamfer the edges of the resulting body. Next, create a second sketch on the XY plane shifted downward and extrude it 24 mm to form a separate feature.
