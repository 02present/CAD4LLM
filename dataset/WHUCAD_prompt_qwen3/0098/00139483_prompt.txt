Create a base shape by extruding a sketch on the XY plane with a 95mm by 91mm bounding box, then chamfer the edges. Add a second extruded feature based on a smaller sketch shifted down by 11mm, both extending 96mm in the same direction.
