Create a series of three extruded cylinders by first sketching circles on the XY plane, then extruding each sketch into a new body with the specified distances. The first circle is smaller and centered within the bounding box, while the subsequent circles are larger and positioned similarly. Each extrusion should be blind and extend the given distance.
