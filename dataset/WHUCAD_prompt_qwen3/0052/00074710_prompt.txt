Create a base shape by extruding a sketch on the XY plane with three lines, then add a second extrusion using a different sketch on the same plane with four lines, followed by a chamfer. Continue by extruding multiple sketches in a vertical direction, each with four lines, and finally create a top surface by extruding a horizontal sketch with six lines.
