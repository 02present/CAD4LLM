Create a base by extruding a circle sketch 32 millimeters to form a cylinder. Then chamfer the edge. Next, sketch three lines on the XY plane and extrude them 90 millimeters to create a separate block.
