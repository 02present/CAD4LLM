Create a base shape by extruding a sketch on the XY plane with four lines, then add another extrusion using a larger sketch on the same plane. Chamfer the edges of the first extrusion and create additional features by extruding more complex sketches at different depths.
