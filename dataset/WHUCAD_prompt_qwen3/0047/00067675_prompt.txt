Create a base shape by sketching a series of lines on the XY plane with a bounding box from 0 to 73 in X and -14 to 95 in Y, then extrude it 4 millimeters to form a new body. Add a chamfer to the edge, and create another sketch with lines on the XY plane within a smaller bounding box, extruding it 4 millimeters to form a second separate body.
