Create a base shape by extruding a circle with a 32 mm diameter along the Z-axis to form a cylinder. Then, add a chamfer to one edge of the cylinder. Next, create a larger cylinder with a 96 mm diameter and extrude it along the Z-axis to form a taller section. Finally, position the smaller cylinder inside the larger one to create a nested cylindrical structure.
