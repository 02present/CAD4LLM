Create a base shape by extruding a sketch on the XY plane with 8 lines, then add a second extrusion using a smaller sketch on the same plane, followed by a chamfer on the edges. Finally, create a third extrusion using another sketch on the XY plane to form the final structure.
