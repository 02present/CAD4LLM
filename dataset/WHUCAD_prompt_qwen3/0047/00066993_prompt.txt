Create a base shape by extruding a sketch on the XY plane with seven lines, then repeat the same sketch and extrusion process to form multiple stacked features. Add a chamfer to one of the edges and create additional extruded features using smaller sketches on the same plane with varying dimensions.
