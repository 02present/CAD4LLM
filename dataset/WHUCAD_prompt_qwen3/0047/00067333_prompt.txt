Create a new body by extruding the sketch along the Z-axis for 96 millimeters, using the sketch defined on the XY plane with arcs and lines forming a symmetrical pattern within the specified bounding box.
