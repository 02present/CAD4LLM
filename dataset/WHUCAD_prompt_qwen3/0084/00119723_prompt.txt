Create a base shape by extruding a sketch on the XY plane with a height of 80 mm, then add another extrusion of 96 mm using a smaller sketch. Apply a chamfer to one of the edges and create two more extrusions of 64 mm each from additional sketches positioned below the initial ones.
