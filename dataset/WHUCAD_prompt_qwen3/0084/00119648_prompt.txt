Create a base shape by extruding a sketch on the XY plane with a 65 mm depth, then add a chamfer to the edge. Next, create a second sketch on the same plane but slightly smaller and extrude it to 92 mm to form a secondary feature.
