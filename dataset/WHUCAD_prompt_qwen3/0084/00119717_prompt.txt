Create a base body by extruding a sketch along the Z-axis for 120 mm, then create a second body by extruding another sketch along the Z-axis for 57 mm. Both sketches are drawn on the XY plane and consist of four lines each.
