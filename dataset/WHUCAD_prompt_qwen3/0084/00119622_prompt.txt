Create a base shape by extruding a sketch on the XY plane with a 95x71mm bounding box, then chamfer the edges. Add a second extrusion of a smaller sketch on the same plane with a 95x24mm bounding box, extruded 48mm deep to form a separate feature.
