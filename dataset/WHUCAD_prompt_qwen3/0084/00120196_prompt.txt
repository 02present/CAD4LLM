Create a base shape by extruding a sketch on the XY plane with a 95mm width and 52mm height, then chamfer the edges. Next, create a second extrusion using a slightly taller sketch on the same plane, extending 127mm to form a separate body.
