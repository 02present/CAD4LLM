Create a base shape by extruding a sketch on the XY plane with a 95mm width and 67mm height, then add a chamfer. Repeat by creating another sketch slightly lower and extrude it to form a second section, followed by a third larger extrusion. Finally, apply a chamfer to the top edge of the final feature.
