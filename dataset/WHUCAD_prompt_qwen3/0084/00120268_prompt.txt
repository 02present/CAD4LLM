Create a base shape by extruding a sketch on the XY plane with a 35 mm depth, then chamfer the edges. Next, create a second extrusion using a smaller sketch on the same plane with a 95 mm depth to form the final structure.
