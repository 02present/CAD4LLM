Create a base shape by extruding a circle from the first sketch 33 millimeters, then chamfer the edge. Next, create a second circular sketch centered on the XY plane and extrude it 31 millimeters to form a separate body.
