Create a base shape by extruding a sketch consisting of seven lines on the XY plane, then create a second extruded feature from a separate sketch with four lines also on the XY plane, both extrusions extending 1 millimeter.
