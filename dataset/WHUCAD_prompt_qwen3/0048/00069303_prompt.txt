Create a base shape by extruding a sketch along the Z-axis for 48 millimeters. Then, chamfer the edges of the resulting body. Next, create a second sketch on the XY plane and extrude it 80 millimeters to form a separate feature.
