Create a base shape by extruding a sketch on the XY plane with dimensions 59mm by 24mm, then add a chamfer. Next, create several additional extrusions using different sketches on the same plane with varying sizes and depths, ensuring each extrusion is separate and distinct.
