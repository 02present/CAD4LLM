Create a base shape by extruding a sketch on the XY plane with four lines, then chamfer the edges. Add two more extrusions based on separate sketches with six lines each, all extending 64 millimeters in the same direction.
