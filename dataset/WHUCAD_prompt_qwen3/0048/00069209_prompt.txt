Create a base shape by extruding a sketch along the Z-axis for 48 millimeters. Then, chamfer the edges of the resulting body. Next, create a second sketch on the same plane and extrude it 72 millimeters to form a separate feature.
