Create a base shape by extruding a sketch on the XY plane with a 95x71mm bounding box, then chamfer the edges. Add a second extruded feature based on a new sketch on the XY plane with a 79x95mm bounding box, both extending 24mm in depth.
