Create a base shape by sketching four lines on the XY plane with a width of 11mm and height of 95mm, then extrude it 1mm to form a solid body. Add two chamfers to the edges, then create a second sketch on the XY plane with smaller dimensions, and extrude it 18mm to form a separate body.
