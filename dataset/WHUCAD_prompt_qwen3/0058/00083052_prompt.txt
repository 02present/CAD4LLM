Create a base shape by extruding a sketch along the Z-axis for 48 mm, then add a chamfer to the edge. On the same plane, create another identical sketch and extrude it 48 mm to form a second body, ensuring both features are aligned and proportionally sized.
