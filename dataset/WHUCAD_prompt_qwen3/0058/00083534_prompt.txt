Create a base shape by extruding a sketch on the XY plane with seven lines, then add another extrusion using a different sketch with five lines, both extending 6 millimeters. Next, create three more extrusions from sketches with varying line and arc combinations, each extending 3 millimeters. Finally, chamfer one of the edges to refine the overall geometry.
