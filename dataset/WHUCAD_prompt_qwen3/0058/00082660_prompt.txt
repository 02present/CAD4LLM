Create a base body by extruding a circle with a 32 mm diameter on the XY plane, then add a chamfer. Next, create two more bodies by extruding circles of the same size but with different positions and larger extrusion distances, ensuring all features are aligned and proportionally spaced.
