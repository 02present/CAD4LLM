Create a base body by extruding a sketch with seven lines on the XY plane, then create a second identical extrusion using a separate sketch with three lines, both extending 62 millimeters in the same direction.
