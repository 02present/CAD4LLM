Create a new body by extruding the sketch along the Z-axis for 126 millimeters, using the sketch defined on the XY plane with eight entities consisting of arcs and lines arranged within the specified bounding box.
