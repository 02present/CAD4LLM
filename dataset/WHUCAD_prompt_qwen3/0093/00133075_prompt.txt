Create a base body by extruding a sketch on the XY plane with four lines forming a rectangle, then create two more identical extrusions using subsequent sketches on the same plane, each offset vertically to form a stacked structure.
