Create a base shape by extruding a sketch along the XY plane for 12 millimeters, then chamfer the edges. On the same plane, sketch another identical shape and extrude it 7 millimeters to form a separate body.
