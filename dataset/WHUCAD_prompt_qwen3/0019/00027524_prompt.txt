Create a base shape by sketching six lines on the XY plane with a width of 95mm and height of 50mm, then extrude it 67mm to form a solid body. Add a chamfer to one edge, and create a second smaller sketch with three lines on the same plane but larger in height, extruding it 2mm to form a separate feature.
