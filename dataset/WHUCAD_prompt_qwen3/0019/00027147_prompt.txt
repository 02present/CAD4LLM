Create a base shape by extruding a circle sketch on the XY plane 64 millimeters to form a solid body. Then, chamfer the edges of this body. Next, create a second circular sketch on the same plane but with a larger bounding box and extrude it 32 millimeters to form another separate solid body.
