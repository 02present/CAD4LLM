Create a base shape by extruding a sketch on the XY plane with a 12 mm depth, then add a chamfer to the edge. Next, create a second sketch slightly larger than the first and extrude it to a depth of 54 mm to form the final structure.
