Create a base shape by extruding a sketch on the XY plane with four lines, then create a larger body by extruding another sketch on the same plane with eight lines. Add a chamfer to one of the edges and finally extrude a smaller sketch on the XY plane to form the final feature.
