Create a base shape by sketching an 8-entity pattern on the XY plane with a 95x95mm bounding box, then extrude it 96mm to form a solid body. Add a chamfer to the edge, and create a second smaller sketch with two entities on the same plane, extruding it 96mm to form a separate body.
