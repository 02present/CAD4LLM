Create a base shape by extruding a sketch on the XY plane with five lines, then add another extrusion using a new sketch with six lines, followed by two chamfers on the edges. Continue by extruding additional sketches at different heights and sizes, ensuring each extrusion is blind and based on its respective sketch. Finally, create a final extruded feature from a sketch that spans vertically across the width.
