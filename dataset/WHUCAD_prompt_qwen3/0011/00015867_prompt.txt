Create a base shape by extruding a circle sketch 11 mm. Then add three separate extrusions from line sketches, each with different lengths, and apply chamfers to three edges. Finally, create a larger square sketch and extrude it 11 mm before applying the last chamfer.
