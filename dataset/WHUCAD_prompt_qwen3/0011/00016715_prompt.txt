Create a series of extruded features by first sketching profiles on the XY plane with varying shapes and dimensions, then extruding each sketch 56 millimeters deep to form separate bodies. For the last two sketches, reduce the extrusion depth to 24 millimeters instead.
