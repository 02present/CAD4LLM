Create a base shape by extruding a sketch on the XY plane with six lines, then add another extrusion using a different sketch with five lines, both extending 18 millimeters. Chamfer the edges of the first extrusion. Continue by creating additional extrusions from new sketches, each extending 13 millimeters, to form the final assembled part.
