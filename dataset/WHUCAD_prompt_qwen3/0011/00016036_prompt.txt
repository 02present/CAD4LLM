Create a base shape by extruding a sketch on the XY plane with a 95x65mm bounding box, then chamfer the edges. Add a second extrusion using a larger XY sketch with an 95x85mm bounding box, extruding 64mm to form a separate body.
