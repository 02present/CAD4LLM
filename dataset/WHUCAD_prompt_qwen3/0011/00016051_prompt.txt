Create a base shape by sketching three lines on the XY plane with a bounding box from 0 to 95 in the X direction and -35 to 65 in the Y direction, then extrude it 89 millimeters to form a new body. Add a chamfer to the edge, and create another sketch with three lines on the XY plane within a smaller bounding box, then extrude it 39 millimeters to form a second new body.
