Create a base shape by extruding a sketch on the XY plane with a 95x29 mm bounding box, then add a chamfer. Next, create a second extrusion using a slightly larger sketch on the same plane, extruding 117 mm to form a separate body.
