Create a base shape by extruding a square sketch 48 mm in the positive Z-direction, then repeat the same sketch and extrusion process three times along the Z-axis to form a stack of identical blocks, and finally chamfer all the edges where the blocks meet.
