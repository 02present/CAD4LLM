Create a base shape by extruding a sketch of five lines on the XY plane to a depth of 64 mm. Then, chamfer the edges. Next, create a circular sketch on the XY plane centered around the origin and extrude it to a depth of 125 mm to form a separate feature.
