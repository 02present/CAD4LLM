Create a base shape by extruding a sketch along the XY plane for 7 millimeters, then chamfer the edges. Next, create another extrusion using a similar sketch but extend it 124 millimeters to form the final part.
