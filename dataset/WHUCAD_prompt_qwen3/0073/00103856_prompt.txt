Create a base shape by extruding a square sketch 96 mm upwards, then create another identical extrusion on top of it. Add a smaller square sketch and extrude it halfway up the height, followed by another smaller sketch extrusion at half the height again. Finally, chamfer all the edges where the shapes meet.
