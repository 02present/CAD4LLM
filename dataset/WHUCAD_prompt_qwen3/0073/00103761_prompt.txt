Create a base shape by extruding a sketch on the XY plane with six lines, then add another extrusion using a larger sketch with twelve lines, followed by a chamfer. Continue by extruding additional sketches at different sizes and positions, ensuring each extrusion is blind and creates a new body. Finally, create the largest extrusion using a sketch with twelve lines and an extended depth of thirty millimeters.
