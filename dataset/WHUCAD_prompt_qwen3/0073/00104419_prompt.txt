Create a base shape by extruding a sketch on the XY plane with four lines, then repeat the same sketch and extrusion process to form three separate bodies, finally adding a chamfer to one of the edges.
