Create a base body by extruding a circle sketch along the Z-axis for 96 millimeters, then create a second separate body by extruding another circle sketch along the Z-axis for 37 millimeters. Both sketches are centered on the same plane and have the same dimensions.
