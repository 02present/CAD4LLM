Create a base shape by extruding a sketch on the XY plane with a 95x87mm bounding box, then chamfer the edges. Next, create a second extrusion using a smaller sketch on the same plane with a 77x95mm bounding box and extrude it 126mm to form the final part.
