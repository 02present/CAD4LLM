Create a base shape by extruding a sketch on the XY plane with four lines, then add two more extrusions using different sketches on the same plane. Finally, apply a chamfer to one of the edges and create a circular feature by extruding a circle on the XY plane.
