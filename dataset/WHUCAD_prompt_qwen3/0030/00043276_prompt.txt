Create a symmetrical part by first sketching an arc and line pattern on the XY plane, then revolving it around the Z-axis to form a 3D shape. Next, create a second identical sketch on the same plane and extrude it 32 millimeters to form a separate body. Ensure both features are aligned and proportionally scaled as specified.
