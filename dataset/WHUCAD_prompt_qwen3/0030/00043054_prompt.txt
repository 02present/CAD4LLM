Create a base shape by extruding a sketch on the XY plane with four lines, then add three more extrusions using similar sketches on the same plane but with different dimensions. Apply chamfers to three specific edges to refine the corners.
