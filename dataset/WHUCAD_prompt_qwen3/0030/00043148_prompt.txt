Create a base body by extruding a sketch on the XY plane with a 95x52mm bounding box, then add two chamfers to the edges. Next, create a smaller sketch on the same plane with a 95x17mm bounding box and extrude it to form a second body, followed by another chamfer. Finally, create a third sketch with a 95x26mm bounding box and extrude it to form the final body.
