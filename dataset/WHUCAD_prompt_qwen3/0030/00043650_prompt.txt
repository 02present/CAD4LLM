Create a symmetrical part by first sketching four lines on the XY plane to form a base shape, then revolving it around the Z-axis to create a 3D form. Next, sketch another set of four lines on the same plane but smaller in width and extrude it 64 millimeters to form a separate body. Finally, chamfer the edges where the two bodies meet for a clean finish.
