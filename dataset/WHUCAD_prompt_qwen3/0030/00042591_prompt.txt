Create a base shape by extruding a sketch on the XY plane with a 95x35mm bounding box 75mm upwards. Then chamfer the edges. Add a second sketch on the same plane but with a larger 95x73mm bounding box and extrude it 125mm upwards to form a separate body.
