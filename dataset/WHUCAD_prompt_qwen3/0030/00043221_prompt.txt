Create a base body by extruding a circle with a 32 mm diameter along the Z-axis. Then, create a second body by extruding a larger circle with a 125 mm diameter along the same axis, ensuring both features are on separate bodies.
