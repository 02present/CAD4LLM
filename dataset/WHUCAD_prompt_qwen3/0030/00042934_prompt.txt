Create a base body by extruding a sketch with eight lines on the XY plane, then create another body by extruding a separate sketch with four lines on the same plane, both extrusions extending 96 millimeters.
