Create a base body by extruding a sketch with four lines on the XY plane, then add a chamfer. On the same plane, create two more extruded features using separate sketches with six and four lines respectively, each extruded 80 millimeters.
