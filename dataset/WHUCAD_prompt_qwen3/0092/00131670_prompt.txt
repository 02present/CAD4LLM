Create a base shape by extruding a sketch on the XY plane with a 95x54mm bounding box, then chamfer the edges. Add a second extruded feature based on a smaller sketch with a 95x37mm bounding box, both extending 20mm in the same direction.
