Create a base shape by sketching an arc and two lines on the XY plane, then extrude it 7 mm to form a solid body. Add a chamfer to one edge, and create another shape by sketching a similar arc and lines on the same plane but smaller dimensions, then extrude it 7 mm as a separate body.
