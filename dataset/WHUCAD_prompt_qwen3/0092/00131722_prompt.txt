Create a base shape by extruding a circle with a 24mm diameter on the XY plane 51mm upwards. Then chamfer the edge. Next, create a second circle with a larger diameter on the same plane but shifted vertically, and extrude it 45mm upwards to form a separate body.
