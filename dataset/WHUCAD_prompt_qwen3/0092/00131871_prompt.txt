Create a base shape by extruding a sketch on the XY plane with a 95mm by 56mm bounding box, then chamfer the edges. Next, create a second extrusion using a new sketch on the XY plane with a 24mm by 95mm bounding box and extrude it 109mm.
