Create a base shape by extruding a sketch on the XY plane with a 40mm by 95mm bounding box, then add a second extrusion using a smaller sketch on the same plane but with a 55mm width, both extending 96mm and 101mm respectively. Finally, chamfer the edges of the resulting structure.
