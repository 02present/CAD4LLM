Create a base feature by extruding a circle sketch 14 millimeters in the XY plane, then chamfer the edge. Next, create a second extrusion using another circle sketch, extending 120 millimeters to form the final shape.
