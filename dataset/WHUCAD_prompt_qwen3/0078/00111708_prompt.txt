Create a base shape by extruding a sketch on the XY plane with five lines, then add a second extrusion using a smaller sketch on the same plane to form a protrusion. Continue by extruding additional sketches at varying heights to build up the structure, and finish by chamfering each corner to give the model a refined edge.
