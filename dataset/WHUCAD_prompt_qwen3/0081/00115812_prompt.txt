Create a base shape by extruding a sketch on the XY plane with five lines, then chamfer the edges. Add another extruded feature using a second sketch on the XY plane with three lines, positioned below the first sketch, both extrusions extending 48 millimeters.
