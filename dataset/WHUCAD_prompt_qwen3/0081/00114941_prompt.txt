Create a base shape by extruding a sketch on the XY plane with six lines, then chamfer the edges. Add a second extruded feature using a smaller sketch on the same plane, offsetting it to form a secondary structure.
