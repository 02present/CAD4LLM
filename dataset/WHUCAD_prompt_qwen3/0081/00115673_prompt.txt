Create a base body by extruding a sketch on the XY plane with six lines, then add two chamfers. Next, create two more extruded features using separate sketches on the XY plane with four lines each, ensuring the second extrusion is slightly shorter than the first. Finally, apply a second chamfer to complete the design.
