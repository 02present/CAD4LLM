Create a base body by extruding a sketch with four lines on the XY plane to a depth of 64 mm. Then, add a chamfer to the edge. Next, create two additional bodies by extruding two more sketches, each with four lines on the XY plane, to depths of 32 mm and 64 mm respectively.
