Create a base shape by extruding a sketch on the XY plane with a 63mm by 95mm bounding box, then chamfer the edges. Add a second extrusion using a larger sketch on the same plane with an 81mm width, both extending 37mm to form a symmetrical structure.
