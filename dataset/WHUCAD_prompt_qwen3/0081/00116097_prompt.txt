Create a symmetrical base shape by extruding six circular sketches along the Z-axis to a depth of 14 mm each, then chamfer the edges to refine the corners. Add additional structural elements by extruding a series of linear profiles across the XY plane, also to a depth of 14 mm, ensuring all features are aligned and proportionally spaced.
