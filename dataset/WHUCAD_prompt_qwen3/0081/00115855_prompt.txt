Create a base shape by extruding a sketch on the XY plane with six lines, then repeat this process using the same sketch dimensions but different positions to form multiple protrusions. Add another set of smaller extrusions along the length, and finally chamfer one of the edges to refine the overall geometry.
