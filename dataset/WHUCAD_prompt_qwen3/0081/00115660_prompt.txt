Create a base shape by extruding a sketch on the XY plane with five lines, then chamfer the edges. Add a second extrusion using a smaller sketch with four lines at a distance of 32 mm. Finally, create a taller extrusion using a larger sketch with three lines and an extrusion height of 96 mm.
