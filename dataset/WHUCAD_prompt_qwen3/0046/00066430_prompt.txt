Create a base shape by extruding a sketch on the XY plane with a 49mm by 95mm bounding box, then add a chamfer. Repeat this process with similar sketches and extrusions, adjusting the bounding box dimensions as needed, and apply another chamfer before finalizing the design.
