Create a base shape by extruding a sketch on the XY plane with six lines and one arc, then create another extrusion using a second sketch with similar dimensions but a larger height. Finally, chamfer the edges of the resulting structure.
