Create a base body by extruding a sketch on the XY plane with four lines, then chamfer the edges. Add two more extrusions using separate sketches on the XY plane, each with three lines, and extrude them 119 mm deep to form the final shape.
