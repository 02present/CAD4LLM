Create a base shape by extruding a series of sketched profiles along the Z-axis with a depth of 26 mm each, then apply chamfers at specified locations to refine the edges. The sketches include arcs and lines arranged on the XY plane, forming a complex pattern of interconnected features.
