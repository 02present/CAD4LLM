Create a base shape by sketching four lines on the XY plane with dimensions 95mm by 54mm, then extrude it 121mm to form a solid body. Add a chamfer to the edge, and create a second sketch with seven lines on the XY plane within a smaller bounding box, then extrude it 1mm to form a separate feature.
