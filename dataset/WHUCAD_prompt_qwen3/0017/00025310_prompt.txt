Create a base shape by extruding a sketch on the XY plane with six lines, then create another identical extrusion slightly smaller and offset, followed by two chamfers on the edges. Finally, add a third extruded feature based on a new sketch with four lines, all at a depth of 13 millimeters.
