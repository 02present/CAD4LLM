Create a base body by extruding a sketch on the XY plane with a 96 mm depth, then add two chamfers to the edges. On the same plane, create a smaller sketch and extrude it 42 mm deep, followed by a final chamfer on the resulting feature.
