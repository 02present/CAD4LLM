Create a base shape by extruding a sketch on the XY plane with four lines, then add another extrusion using a second sketch with six lines on the same plane but extended vertically. Finally, chamfer the edges of the resulting structure.
