Create a base shape by extruding a series of sketched lines on the XY plane, each extruded 72 mm in the Z direction. Apply chamfers at specified locations after each extrusion to refine the edges. Repeat the process with varying sketch dimensions to build the final complex form.
