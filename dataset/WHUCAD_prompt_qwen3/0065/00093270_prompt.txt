Create a base shape by extruding a sketch on the XY plane with a 95x76mm bounding box, then chamfer the edges. Next, create a second extrusion using a new sketch on the XY plane with a 93x95mm bounding box, extruding it 48mm to form a separate body.
