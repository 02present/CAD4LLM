Create a base body by extruding a circle with a 48 mm diameter along the Z-axis. Then, create a second body by extruding another circle with an 80 mm diameter along the Z-axis, positioning it so its center is offset from the first circle in the Y-direction.
