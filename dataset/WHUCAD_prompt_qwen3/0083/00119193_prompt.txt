Create a base shape by extruding a square sketch 2mm tall, then add another identical extrusion on top with a larger square sketch. Next, create a taller extrusion using a slightly larger square sketch. Finally, chamfer the edges and add a circular hole through the center with a deep extrusion.
