Create a base body by extruding a circle sketch along the Z-axis for 96 mm. Then chamfer the edges. Add another extrusion using two arc sketches on the XY plane, each extruded 96 mm to form a secondary feature.
