Create a base shape by extruding a sketch on the XY plane with four lines, then add a second extrusion using a different sketch on the same plane with six lines, both extending 14 millimeters. Chamfer the edges of the first extrusion and create a third extrusion using another identical sketch to form the final structure.
