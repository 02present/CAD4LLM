Create a new body by extruding the sketch along the Z-axis for 24 millimeters, using the lines drawn on the XY plane within the specified bounding box. The sketch consists of twelve lines arranged to form the base shape of the model.
