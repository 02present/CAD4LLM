Create a base shape by extruding a sketch on the XY plane with a 95x48mm bounding box, then add another extrusion using a different sketch on the same plane but with a 72x95mm bounding box, both extending 21mm. Finally, chamfer the edges of the resulting structure.
