Create a base shape by extruding a sketch on the XY plane with a 77 mm depth, then add two chamfers to the edges. Next, create a second sketch on the same plane and extrude it 38 mm to form a separate feature.
