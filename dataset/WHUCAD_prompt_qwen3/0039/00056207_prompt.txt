Create a base shape by extruding a sketch on the XY plane with a 95x55mm bounding box, then chamfer the edges. Next, create a second extrusion using a new sketch on the XY plane with a 93x95mm bounding box, forming a separate body.
