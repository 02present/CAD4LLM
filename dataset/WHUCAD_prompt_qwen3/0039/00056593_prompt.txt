Create a base shape by extruding a sketch along the XY plane with a 96 mm depth. Add a chamfer to one edge, then create a second extruded feature based on a slightly smaller sketch also extruded 96 mm deep.
