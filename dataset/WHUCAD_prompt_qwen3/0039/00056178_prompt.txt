Create a base shape by extruding a sketch on the XY plane with a height of 82 mm. Add chamfers to two edges, then create additional extruded features using subsequent sketches on the same plane, each also extruded 82 mm. Ensure all sketches are properly defined with lines forming the required bounding boxes.
