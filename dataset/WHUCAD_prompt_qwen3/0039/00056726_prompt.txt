Create a base body by extruding a sketch along the Z-axis for 96 mm, then create a second identical body using another sketch positioned slightly smaller and extruded the same distance. Add a chamfer to one of the edges where the two bodies meet. Finally, create a third extrusion from a shorter sketch along the Z-axis for 96 mm to form a top feature.
