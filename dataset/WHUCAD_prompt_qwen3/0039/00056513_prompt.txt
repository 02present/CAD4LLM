Create a base shape by extruding a sketch on the XY plane with a 46x95mm bounding box, then repeat this process four times with slightly adjusted sketches and extrude each to 38mm, adding a chamfer to each resulting feature.
