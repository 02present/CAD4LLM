Create a base shape by extruding a sketch on the XY plane with a 72 mm depth, then add a chamfer to the edge. Next, create a second sketch slightly larger than the first and extrude it with a 59 mm depth to form a separate body.
