Create a base body by extruding a circle sketch along the Z-axis for 96 millimeters. Then, chamfer the edge of the resulting body. Next, create a second circular sketch on the same plane and extrude it upward for 113 millimeters to form a separate feature.
