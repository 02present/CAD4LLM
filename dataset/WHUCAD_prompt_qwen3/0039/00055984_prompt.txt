Create a base shape by extruding a sketch on the XY plane with six lines, then chamfer the edges. Add a second extruded feature based on a smaller sketch on the XY plane with three lines, positioned below the first one, and extrude it 64 mm.
