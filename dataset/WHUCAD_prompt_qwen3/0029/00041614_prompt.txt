Create a base shape by extruding a sketch on the XY plane with five lines, then create another extrusion using an 8-line sketch on the same plane but rotated, followed by two chamfers on the edges. Add more features by extruding additional sketches at different distances, ensuring each extrusion is blind and creates a new body. Finally, include arc-based sketches for the last two extrusions to form the final complex shape.
