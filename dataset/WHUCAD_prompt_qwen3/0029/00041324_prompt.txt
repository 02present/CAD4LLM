Create a base shape by extruding a sketch on the XY plane with seven lines, then add a chamfer. Next, create a smaller extrusion using a five-line sketch on the same plane and apply another chamfer. Finally, form a larger extrusion from a four-line sketch on the XY plane, ensuring all features are properly aligned and dimensioned.
