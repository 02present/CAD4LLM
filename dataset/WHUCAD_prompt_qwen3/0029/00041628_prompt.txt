Create a base shape by extruding a sketch along the XY plane with a 47 mm depth, then repeat the same sketch on two additional planes and extrude each to form three parallel walls, finally chamfering the edges where they meet.
