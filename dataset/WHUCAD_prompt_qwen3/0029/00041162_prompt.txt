Create a base shape by extruding a sketch on the XY plane with a 64 mm depth, then add a chamfer to the edge. Next, create a larger sketch on the same plane and extrude it 96 mm to form a second body.
