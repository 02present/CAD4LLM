Create a base shape by extruding a sketch on the XY plane with a height of 77 mm. Then, add a chamfer to the edge. Next, create a smaller protrusion by extruding another sketch on the XY plane to a height of 51 mm. Finally, form a larger feature by extruding a third sketch on the XY plane to the same height as the first extrusion.
