Create a base body by extruding a sketch on the XY plane with 8 lines over 96 mm. Chamfer the edges, then add three more extruded features based on smaller sketches, each extruded 96 mm, followed by another chamfer. Finally, create two additional extrusions using smaller sketches, one at half the height and one at a shorter depth.
