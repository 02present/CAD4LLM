Create a base feature by extruding a circle from the first sketch 26 millimeters, then chamfer the edge. Add two more extrusions using circles from subsequent sketches, each extending 123 millimeters, to form a symmetrical stack. Ensure all features are aligned on the same plane.
