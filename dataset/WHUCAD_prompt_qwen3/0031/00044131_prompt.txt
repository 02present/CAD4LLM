Create a base shape by sketching twelve lines on the XY plane within the specified bounds, then revolve that sketch around an axis to form a symmetrical 3D object. Next, add a circular sketch on the XY plane at the specified location and extrude it 13 millimeters to create a separate body.
