Create a base shape by extruding a sketch on the XY plane with four lines, then repeat the same sketch and extrusion process to form a symmetrical structure. Add chamfers to the edges and create additional features by extruding new sketches with varying dimensions and shapes to build the final complex form.
