Create a base body by extruding the first sketch 96 mm along the Z-axis, then create a second identical extrusion using the second sketch, also 96 mm along the Z-axis, resulting in two separate bodies stacked on top of each other.
