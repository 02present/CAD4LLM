Create a base shape by sketching an arc and lines on the XY plane with a bounding box from 0 to 95 in X and -42 to 85 in Y, then extrude it 96 mm to form a new body. Add two chamfers to the edges, then create a second sketch with arcs and lines on the XY plane within a smaller bounding box and extrude it 96 mm to form another new body.
