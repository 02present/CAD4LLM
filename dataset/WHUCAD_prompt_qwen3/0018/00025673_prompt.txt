Create a base shape by sketching 14 lines on the XY plane with dimensions 93mm by 34mm, then extrude it 46mm to form a solid body. Add a chamfer to the edge, and create a second smaller feature by sketching another set of 14 lines slightly larger than the first, then extrude it 2mm to form a separate body.
