Create a base shape by extruding a sketch on the XY plane with a 73x43mm bounding box, then add a chamfer. Next, create a second extrusion using a larger sketch on the same plane with a 95x54mm bounding box, also extruded 96mm in height.
