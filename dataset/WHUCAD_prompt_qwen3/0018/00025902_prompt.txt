Create a base shape by extruding a sketch on the XY plane with dimensions 48mm by 93mm, then extrude another sketch of 95mm by 1mm to form a protrusion. Add two chamfers to the edges and create additional extrusions using similar sketches to build the final structure.
