Create a base shape by sketching an 8-entity pattern of arcs and lines on the XY plane within a 93x93mm bounding box, then extrude it 127mm to form a solid body. On the same plane, sketch a square with four lines inside a 94x94mm area and extrude it 7mm to create a smaller attached feature. Finally, chamfer the edges where the two features meet.
