Create a base shape by extruding a sketch on the XY plane with a 95x39mm bounding box 6mm upwards. Then, create two additional features by extruding two more sketches, one with a 28x95mm bounding box and another with an 85x95mm bounding box, both 16mm upwards to form a stacked assembly.
