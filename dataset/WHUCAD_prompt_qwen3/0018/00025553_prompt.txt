Create a base shape by extruding a sketch on the XY plane with six lines, then chamfer the edges. Add another extruded feature based on a second sketch, positioned slightly lower, to form the final structure.
