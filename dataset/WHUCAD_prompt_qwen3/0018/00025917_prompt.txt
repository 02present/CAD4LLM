Create a base body by extruding a sketch along the Z-axis for 119 mm, then add a chamfer to the edge. On the same plane, sketch another identical shape and extrude it 21 mm to form a separate feature.
