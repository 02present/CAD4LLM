Create a base shape by extruding a sketch consisting of five lines on the XY plane, then chamfer the edges. Next, create a circular sketch on the XY plane within the specified bounds and extrude it to form a second feature, followed by another chamfer on the resulting edges.
