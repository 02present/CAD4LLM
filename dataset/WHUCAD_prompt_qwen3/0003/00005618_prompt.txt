Create a base shape by extruding a sketch on the XY plane with four lines, then create another extrusion using a new sketch with five lines, followed by a chamfer. Repeat this process with additional sketches and extrusions, ensuring each extrusion has a depth of 120 mm except the last one, which should be 3 mm deep.
