Create a base shape by sketching six lines on the XY plane with a width of 95mm and height of 27mm, then extrude it 13mm to form a solid body. Add a chamfer to the edges, then create a second sketch with four lines on the same plane but slightly taller, extrude it 13mm as a new body, and apply another chamfer to the resulting edges.
