Create a base shape by extruding a sketch with four lines on the XY plane, then chamfer the edges. Repeat this process by creating additional sketches with increasing height and extruding each one, applying a chamfer after each extrusion to form a stepped structure.
