Create a base shape by extruding a circle with a 60x60 mm bounding box on the XY plane 87 mm upwards. Then, chamfer the edges. Next, create a second circular sketch with a 96x48 mm bounding box on the XY plane and extrude it 72 mm upwards to form a separate body.
