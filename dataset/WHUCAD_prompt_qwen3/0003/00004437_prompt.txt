Create a base shape by extruding a sketch on the XY plane with four lines, then create another extrusion using a slightly smaller sketch on the same plane, and finally add a chamfer to the edges. Add a third extrusion using a larger sketch with an arc, all with a depth of 16 millimeters.
