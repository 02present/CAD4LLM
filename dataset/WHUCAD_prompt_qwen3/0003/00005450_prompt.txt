Create a base shape by extruding a sketch on the XY plane with 14 lines, then add two chamfers. Next, create additional features by extruding four separate sketches on the XY plane, each forming a smaller section, and finally apply another chamfer to complete the design.
