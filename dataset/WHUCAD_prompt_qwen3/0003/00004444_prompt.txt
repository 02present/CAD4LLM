Create a base shape by sketching four lines on the XY plane within a 95x95 mm square, then extrude it 4 mm to form a solid body. Add a chamfer to the edge, and create a second sketch with eight lines on the XY plane, slightly offset, then extrude this new sketch 124 mm to form a separate body.
