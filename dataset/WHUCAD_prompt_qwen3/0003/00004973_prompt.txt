Create a base shape by extruding a sketch on the XY plane with a 95x63 mm bounding box, then chamfer the edges. Next, create a second extrusion using a smaller sketch on the XY plane with a 48x95 mm bounding box and extrude it 32 mm.
