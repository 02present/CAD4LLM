Create a base shape by extruding a sketch on the XY plane with a 95x71mm bounding box, then add a chamfer. Next, create two more extrusions using separate sketches on the XY plane, one with a 95x95mm bounding box and another with a 63x95mm bounding box, both extruded 80mm in height.
