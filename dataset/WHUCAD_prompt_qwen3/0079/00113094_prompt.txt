Create a base shape by extruding a sketch on the XY plane with a 47mm by 89mm bounding box, then chamfer the edges. Repeat the process to create a second extruded body of the same dimensions and add a 9mm chamfer to the final model.
