Create a base shape by extruding a sketch on the XY plane with four lines, then add a second extrusion on the same plane using five lines at a shorter depth. Chamfer the edges of the first extrusion and create a third extrusion on a rotated plane with another set of five lines for a smaller feature.
