Create a base body by extruding a sketch on the XY plane with six lines, then create another identical extrusion on the same plane, and finally add a chamfer to the edges. Add a small protrusion by extruding a new sketch on the XY plane with four lines, all extrusions being 96 mm deep.
