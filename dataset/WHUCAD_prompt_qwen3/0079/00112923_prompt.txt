Create a base shape by sketching an arc and lines on the XY plane with a bounding box from 0 to 95 in X and -25 to 0 in Y, then extrude it 8 mm to form a new body. Add a chamfer to the edge, then sketch another set of arcs and lines on the XY plane within a smaller bounding box and extrude that as well.
