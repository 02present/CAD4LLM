Create a base feature by extruding a circle with a 24mm diameter along the XY plane for 3mm, then chamfer the edge. Next, add a second extruded feature by creating a smaller circle near the top of the part and extruding it 3mm to form a separate body.
