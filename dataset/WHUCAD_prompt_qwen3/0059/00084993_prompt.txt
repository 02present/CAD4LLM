Create a base shape by extruding a sketch on the XY plane with five lines, then chamfer the edges. On the same plane, sketch another shape with four lines and extrude it to form a second body, both extending 96 millimeters.
