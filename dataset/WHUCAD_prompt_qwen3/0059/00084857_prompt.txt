Create a base shape by extruding a square sketch 51 mm tall. Then, add two smaller extrusions of 8 mm each on top of the base, using identical square sketches. Finally, create a circular hole by extruding a circle sketch 8 mm deep.
