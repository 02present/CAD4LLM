Create a base shape by extruding a sketch consisting of four lines on the XY plane with a bounding box of 87x95 mm, then chamfer the edges. Next, create a second extrusion using a sketch with an arc and two lines on the XY plane, bounded by 92x65 mm, and extrude it 59 mm to form a separate body.
