Create a base shape by extruding a sketch on the XY plane with a 95x75mm bounding box 13mm upwards. Then chamfer the edges. Add another sketch on the same plane but with a smaller 95x66mm bounding box and extrude it 17mm upwards to form a second feature.
