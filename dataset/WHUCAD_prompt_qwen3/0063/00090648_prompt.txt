Create a base shape by extruding a sketch on the XY plane with a 64 mm depth. Add two chamfers to the edges. Then create a smaller protrusion by extruding another sketch on the XY plane with a 13 mm depth and apply a second chamfer. Finally, add a taller feature by extruding a third sketch on the XY plane with a 5 mm depth.
