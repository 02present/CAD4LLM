Create a base shape by extruding a sketch on the XY plane with a 95x59mm bounding box, then chamfer the edges. Next, create a second extrusion using a new sketch on the XY plane with a 41x95mm bounding box and extrude it 126mm.
