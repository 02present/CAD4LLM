Create a base shape by extruding a sketch on the XY plane with six lines, then create another extrusion using a new sketch with seven lines, followed by a chamfer. Repeat this process with additional sketches and extrusions, each time using a new sketch on the XY plane with fewer lines and slightly smaller dimensions, all extruded 31 millimeters deep.
