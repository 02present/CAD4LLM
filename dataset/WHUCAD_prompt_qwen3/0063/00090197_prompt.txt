Create a base shape by extruding a sketch on the XY plane with a 48 mm depth. Then, add a chamfer to the edge. Next, create a smaller protrusion by extruding another sketch on the XY plane with a 16 mm depth. Finally, add a taller feature by extruding a third sketch on the XY plane with a 32 mm depth.
