Create a base shape by extruding a sketch on the XY plane with a 95x83mm bounding box, then chamfer the edges. Add a second extrusion using a smaller sketch on the same plane with a 95x61mm bounding box, extending it 126mm to form a taller section.
