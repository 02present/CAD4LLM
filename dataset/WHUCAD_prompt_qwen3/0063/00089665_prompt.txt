Create a base body by extruding a sketch on the XY plane with a 96 mm depth, then add two chamfers to the edges. Next, create a second extruded feature using a new sketch on the XY plane with a 124 mm depth, ensuring both features are distinct and properly oriented.
