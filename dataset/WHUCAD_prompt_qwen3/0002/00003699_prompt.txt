Create a base shape by extruding a circle from the first sketch 64 millimeters, then chamfer the edge. On the same plane, sketch another circle and extrude it 32 millimeters to form a second feature.
