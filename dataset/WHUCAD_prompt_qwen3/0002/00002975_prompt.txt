Create a base shape by extruding a sketch with four lines on the XY plane, then add another extrusion using a sketch with four lines and an arc, followed by a third extrusion based on a sketch with two arcs and two lines. Finally, revolve a new sketch with six elements around an axis to complete the model.
