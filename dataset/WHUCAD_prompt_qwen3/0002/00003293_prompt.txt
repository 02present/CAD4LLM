Create a base feature by extruding a circle with an 8 mm diameter along the Z-axis for 8 mm. Then, chamfer the edge of this feature. Next, create a second circular sketch centered on the origin with a larger diameter and extrude it 16 mm along the Z-axis to form a separate body.
