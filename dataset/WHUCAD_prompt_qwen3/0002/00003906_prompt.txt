Create a base shape by sketching a rectangle and two lines with an arc on the XY plane, then extrude it 21 millimeters. Add a chamfer to the edge, and create a circular sketch on the XY plane shifted downward, extruding it 59 millimeters to form a separate body.
