Create a base feature by extruding a small circle along the XY plane for 22 millimeters, then chamfer the edge. Next, create a larger circular sketch on the same plane but with a wider bounding box and extrude it 84 millimeters to form the main body.
