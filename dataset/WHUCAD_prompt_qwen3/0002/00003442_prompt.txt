Create a base shape by extruding a sketch on the XY plane with four lines, then chamfer the edges. Repeat this process using the same sketch pattern but rotated to create symmetrical features along the X and Y axes, ensuring each extrusion is 96 millimeters deep.
