Create a base body by extruding a circle with a 48 mm diameter along the Z-axis. Then, chamfer the edge of the base body. Next, create a second circular sketch on the XY plane with a smaller size and extrude it 40 mm to form a secondary feature.
