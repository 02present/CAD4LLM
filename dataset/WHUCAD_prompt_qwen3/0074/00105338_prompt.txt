Create a base shape by extruding a sketch with four lines on the XY plane, then chamfer the edges. Add three more extrusions based on subsequent sketches, each positioned and sized to fit within the specified dimensions. Ensure all extrusions are blind with a depth of 31 millimeters.
