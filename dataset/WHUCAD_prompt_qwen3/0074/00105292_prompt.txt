Create a base shape by extruding a sketch on the XY plane with a 90 mm depth, then add a chamfer to the edges. Next, create a second sketch centered vertically and extrude it 19 mm deep, followed by another chamfer on the resulting edges.
