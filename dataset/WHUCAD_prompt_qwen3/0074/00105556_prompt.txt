Create a base shape by extruding a sketch on the XY plane with a width of 52mm and height of 91mm, then add a chamfer. Next, create a second extrusion using a slightly larger sketch on the same plane, with a width of 53mm and height of 95mm, and extrude it 5mm deep to form a separate feature.
