Create a base shape by extruding a sketch on the XY plane with four lines, then repeat the same sketch and extrusion process to form a second body. Add a chamfer to the edges and create a third extruded feature using a new sketch with six lines on the XY plane.
