Create a base body by extruding a sketch along the XY plane for 126 mm, then add two chamfers to the edges. Next, create a smaller protrusion by extruding another sketch 6 mm and a thin rib by extruding a third sketch 2 mm.
