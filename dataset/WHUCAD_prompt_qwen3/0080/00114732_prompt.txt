Create a base shape by extruding a sketch on the XY plane with four lines, then add three chamfers. Repeat the process with another set of four lines and extrude again, followed by two more chamfers. Finally, create a circular sketch and extrude it to form a smaller cylindrical feature.
