Create a base shape by extruding a sketch on the XY plane with a 95x63mm bounding box, then chamfer the edges. Add a second extrusion of 18mm height using a smaller sketch on the XY plane with a 24x95mm bounding box.
