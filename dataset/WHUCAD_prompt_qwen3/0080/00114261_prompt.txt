Create a base shape by sketching an 8-line and arc profile on the XY plane with a 95x95mm bounding box, then extrude it 9mm to form the main body. Add chamfers to three edges, then create smaller sketches on the XY plane for additional features, each extruded 7mm to form separate parts, and apply final chamfers to complete the design.
