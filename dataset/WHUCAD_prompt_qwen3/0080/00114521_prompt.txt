Create a base shape by extruding a sketch on the XY plane with a 80 mm depth, then chamfer the edges. Add a second extrusion of 47 mm depth using a new sketch on the same plane but with a slightly wider dimension.
