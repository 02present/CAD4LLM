Create a base body by extruding a circle sketch along the Z-axis for 96 millimeters. Then, chamfer the edge of the resulting body. Next, create a smaller circular sketch on the XY plane and extrude it 1 millimeter to form a secondary feature.
