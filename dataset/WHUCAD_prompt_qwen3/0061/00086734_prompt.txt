Create a base shape by extruding a sketch on the XY plane with five lines, then chamfer the edges. Next, create a second extrusion using a new sketch with three lines on the same plane, but extend it shorter to form a secondary feature.
