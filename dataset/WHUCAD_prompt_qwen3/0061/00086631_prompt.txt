Create a base shape by extruding a sketch on the XY plane with a 76x95mm bounding box 59mm upwards. Then chamfer the edges, followed by adding two smaller extrusions of 1mm and 2mm height using separate sketches on the same plane. Finally, apply another chamfer to the final features.
