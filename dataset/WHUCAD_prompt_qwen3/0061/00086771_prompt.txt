Create a base body by extruding a sketch on the XY plane with four lines, then create two more bodies by extruding separate sketches on the same plane with three lines each, using extrusion distances of 80 mm and 99 mm respectively.
