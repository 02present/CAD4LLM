Create a symmetrical base shape by extruding multiple sketched profiles along the Z-axis, then chamfer the edges to refine the corners. Each extrusion should be 21mm or 25mm deep depending on the sketch, and all sketches should be aligned on the XY plane with consistent dimensions.
