Create a base body by extruding a sketch along the Z-axis for 96 mm, then add a chamfer to the edge. On the same XY plane, sketch another identical shape and extrude it 32 mm to form a secondary feature.
