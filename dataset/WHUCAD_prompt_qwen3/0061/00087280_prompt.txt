Create a base shape by extruding a sketch along the Z-axis for 48 millimeters. Then, chamfer the edges of the resulting body. Next, create a second sketch identical to the first and extrude it along the Z-axis for 96 millimeters to form a separate body.
