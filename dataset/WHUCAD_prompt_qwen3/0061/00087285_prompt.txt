Create a base shape by extruding a sketch along the Z-axis for 75 mm, then add a chamfer to the edge. On the same plane, create another sketch and extrude it 50 mm to form a second feature, ensuring both sketches have the same dimensions and layout.
