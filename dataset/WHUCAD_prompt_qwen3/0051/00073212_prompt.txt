Create a base shape by sketching three lines on the XY plane with a bounding box from [0.0, -67.0, 95.0, 0.0], then extrude it 64 mm to form a new body. Chamfer the edges of the resulting feature. Repeat the process by sketching two more sets of three lines on the XY plane with different bounding boxes and extrude each to create three separate bodies.
