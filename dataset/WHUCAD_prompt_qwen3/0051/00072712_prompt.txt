Create a base body by extruding a circle sketch along the Z-axis for 64 mm. Then, chamfer the edges of the resulting feature. Next, create a second circular sketch slightly larger than the first and extrude it similarly to form a second body.
