Create a base shape by extruding a sketch on the XY plane with five lines, then repeat the process with similar sketches to form multiple extruded bodies. Add chamfers to two edges and create additional extrusions from smaller sketches positioned along the same plane.
