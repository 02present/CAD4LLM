Create a base body by extruding a sketch on the XY plane with a 95x36mm bounding box 80mm upwards. Then, chamfer the edges. Add two more extrusions, each 96mm tall, based on two separate sketches on the XY plane with a 54x95mm bounding box.
