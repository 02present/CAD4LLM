Create a base shape by extruding a sketch along the Z-axis for 32 mm, then add a chamfer to the edge. Repeat this process twice more using identical sketches and extrusions to form three separate bodies stacked vertically. Ensure all sketches are aligned on the same XY plane with consistent dimensions.
