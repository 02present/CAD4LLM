Create a base feature by extruding a circle sketch 1.0 mm to form a cylinder, then chamfer its edges. On the same plane, sketch four lines and extrude them 1.0 mm each to create three separate rectangular features, and finally chamfer the outer corners of the last extrusion.
