Create a base shape by extruding a sketch on the XY plane with six lines, then create two more similar extrusions using subsequent sketches, each with a different depth of 32mm and 41mm respectively.
