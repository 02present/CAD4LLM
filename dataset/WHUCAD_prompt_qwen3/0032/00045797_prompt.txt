Create a base shape by extruding a sketch on the XY plane with six lines, then create another extrusion using a new sketch on the same plane with six lines, followed by two more extrusions using subsequent sketches, each with different dimensions, resulting in four separate extruded bodies.
