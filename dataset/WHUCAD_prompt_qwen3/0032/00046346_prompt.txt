Create a base shape by extruding a sketch on the XY plane with four lines, then chamfer the edges. Add a second extrusion using an arc and lines on the same plane but shifted downward, followed by another extrusion using a combination of lines and arcs. Finally, chamfer the final edges of the last extruded feature.
