Create a base body by extruding a sketch on the XY plane with a 32 mm depth, then add two chamfers to the edges. Next, create a second extrusion using a smaller sketch on the same plane, extending 110 mm to form a separate body.
