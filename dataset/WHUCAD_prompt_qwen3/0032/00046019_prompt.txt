Create a base shape by extruding a sketch on the XY plane with five lines, then chamfer two edges. On the same plane, create a smaller sketch with four lines and extrude it separately to form a second feature.
