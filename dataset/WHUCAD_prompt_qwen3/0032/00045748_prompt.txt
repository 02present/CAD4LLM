Create a base shape by extruding a sketch on the XY plane with a 95x76mm bounding box, then chamfer the edges. Next, create a second extrusion using a smaller sketch on the XY plane with a 48x95mm bounding box and extrude it 51mm.
