Create a base shape by extruding a sketch on the XY plane with a 95x55mm bounding box, then chamfer the edges. Add a second extrusion using a lower sketch on the XY plane shifted down by 90mm, with a 95x42mm bounding box.
