Create a base shape by extruding a sketch on the XY plane with four lines, then create another extrusion from a second sketch with three lines, both extending 96 millimeters. Finally, chamfer the edges of the resulting structure.
