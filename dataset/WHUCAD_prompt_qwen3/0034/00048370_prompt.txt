Create a base shape by extruding a sketch on the XY plane with four lines, then add a second extrusion using a larger sketch with six lines, followed by two chamfers on the edges. Finally, create a third extrusion from a smaller sketch and apply another chamfer to complete the design.
