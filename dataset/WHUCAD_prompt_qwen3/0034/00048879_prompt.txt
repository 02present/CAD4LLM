Create a base shape by extruding a sketch on the XY plane with four lines, then chamfer two edges. Repeat this process with additional sketches and extrusions, each time adjusting the sketch geometry slightly and applying a chamfer to the resulting features.
