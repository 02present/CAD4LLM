Create a base shape by extruding a series of sketched lines on the XY plane, with each extrusion extending 20mm except the last ones which extend 104mm, forming multiple stacked features. Add a central circular sketch and extrude it through the full height of 104mm. Include additional smaller line-based sketches along the sides and extrude them similarly to build the final assembly.
