Create a base shape by sketching four lines on the XY plane within a 95x50 mm bounding box, then extrude it 96 mm to form a solid body. Next, sketch an arc and a line on the same plane to define a revolved feature, and revolve that sketch around an axis to complete the model.
