Create a base shape by extruding a sketch on the XY plane with a 32 mm depth, then add a chamfer to the edge. Next, create a second sketch on the XY plane with a slightly different bounding box and extrude it 64 mm deep to form a separate body.
