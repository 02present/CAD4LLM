Create a base shape by extruding a sketch with four lines and an arc on the XY plane to a depth of 23 mm. Add chamfers to two corners, then extrude additional sketches with varying shapes and sizes to the same depth, ensuring all features are aligned on the XY plane.
