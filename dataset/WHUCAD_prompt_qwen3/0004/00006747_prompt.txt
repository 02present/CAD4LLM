Create a base feature by extruding a circle sketch 3 millimeters to form a small protrusion, then chamfer the edge. Next, create a larger protrusion by extruding another circle sketch 96 millimeters, ensuring both features are on the same plane and aligned.
