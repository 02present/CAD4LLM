Create a base shape by extruding a sketch on the XY plane with four lines, then repeat the same sketch and extrusion process to form a second identical body. Chamfer two of the edges, and add two more extruded features from smaller sketches on the XY plane, each with ten lines, and chamfer one of those as well.
