Create a base shape by extruding a sketch on the XY plane with a 53mm by 95mm bounding box 18mm upwards. Then chamfer the edges. Next, create a second sketch on the XY plane with a 95mm by 68mm bounding box and extrude it 55mm upwards to form a separate body.
