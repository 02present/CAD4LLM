Create a base body by extruding a sketch along the Z-axis for 64 mm, then add chamfers to the edges, and repeat the extrusion process using identical sketches positioned along the same axis to form a series of stacked features.
