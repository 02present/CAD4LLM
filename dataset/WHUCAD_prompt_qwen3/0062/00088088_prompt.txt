Create a base shape by extruding a sketch on the XY plane with a 95x40mm bounding box 67mm upwards. Then chamfer the edges. Add two more extrusions, each 32mm high, based on separate sketches placed on the XY plane with a 11x95mm bounding box.
