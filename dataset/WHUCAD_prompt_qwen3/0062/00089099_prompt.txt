Create a base shape by extruding a sketch on the XY plane with a 95mm by 81mm bounding box, then chamfer the edges. Add a second extrusion using a slightly smaller sketch on the same plane, extruding 93mm to form a secondary feature.
