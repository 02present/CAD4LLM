Create a base body by extruding a sketch on the XY plane with a 57 mm depth, then add a chamfer. Next, create two additional bodies by extruding two separate sketches on the XY plane each with an 89 mm depth.
