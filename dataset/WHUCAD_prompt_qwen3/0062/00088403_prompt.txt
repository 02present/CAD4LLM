Create a base shape by extruding a sketch on the XY plane with a 95x60mm bounding box, then chamfer the edges. Add a second extrusion using a new sketch on the XY plane with a 51x95mm bounding box, extruding 88mm to form a separate body.
