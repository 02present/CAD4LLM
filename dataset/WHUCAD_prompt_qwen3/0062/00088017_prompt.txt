Create a base shape by extruding a sketch on the XY plane with dimensions 57mm by 95mm, then add two chamfers to the edges. Next, create a smaller protrusion by extruding a sketch that includes an arc, and finally add a taller protrusion using a narrower sketch, finishing with a third chamfer on the top edge.
