Create a base shape by extruding a sketch consisting of four lines on the XY plane with a bounding box of 58x95 mm, then chamfer the edges. Next, create a second extrusion using a smaller sketch with three lines on the same plane but a narrower bounding box of 40x95 mm, extruding it 104 mm to form a separate body.
