Create a base body by extruding a circle with a 96 mm diameter along the Z-axis. Then, chamfer the edge of the base body. Next, create a smaller circular sketch on the XY plane and extrude it 6 mm to form a secondary feature.
