Create a base body by extruding a sketch on the XY plane with four lines, 23mm deep. Repeat this process with another identical sketch to form a second body, then add a chamfer to the edges. Create additional bodies using different sketches on the XY plane with varying dimensions and extrusion depths, and apply a second chamfer to complete the design.
