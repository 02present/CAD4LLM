Create a base shape by extruding a sketch on the XY plane with a 95x85mm bounding box, then chamfer the edges. Add a second extruded feature based on a new sketch with a 88x95mm bounding box, extruding 102mm to form a separate body.
