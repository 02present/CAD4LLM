Create a base shape by extruding a sketch on the XY plane with a 95x71mm bounding box 16mm upwards. Then chamfer the edges. Add a smaller sketch on the same plane with a 95x31mm bounding box and extrude it 13mm upwards to form a separate feature.
