Create a base by extruding a circle sketch 56 millimeters to form a cylinder. Then, create a second extrusion using three lines sketched on the XY plane, extending 85 millimeters to form a separate block. Finally, chamfer the edges where the two features meet.
