Create a base shape by extruding a sketch on the XY plane with a 57mm by 95mm bounding box, then chamfer the edges. Next, create a smaller extrusion based on another XY sketch with a 36mm by 95mm bounding box and extrude it 32mm.
