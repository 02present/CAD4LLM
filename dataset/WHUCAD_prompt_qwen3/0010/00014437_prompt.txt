Create a base shape by extruding a sketch on the XY plane with a 64 mm depth, then add two chamfers to the edges. Next, create a second sketch slightly smaller than the first and extrude it with a 66 mm depth to form a separate body.
