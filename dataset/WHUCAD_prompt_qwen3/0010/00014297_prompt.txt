Create a base shape by extruding a sketch on the XY plane with five lines, then repeat the same sketch and extrusion process to form a second body. Add chamfers to three selected edges to create a refined corner finish.
