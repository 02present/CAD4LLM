Create a base body by extruding a sketch with seven lines on the XY plane, then create two more bodies by extruding separate sketches with four lines each, positioned along the same plane but with different dimensions, ensuring all extrusions are 23 millimeters deep.
