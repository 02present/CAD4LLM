Create a base shape by extruding a sketch along the Z-axis for 51 millimeters. Then, add a chamfer to one of the edges. On the same plane, create another sketch and extrude it 31 millimeters to form a second feature.
