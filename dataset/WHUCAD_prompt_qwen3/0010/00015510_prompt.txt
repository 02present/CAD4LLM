Create a base shape by sketching six lines on the XY plane within a 95mm wide and 86mm tall bounding box, then extrude it 11mm to form a solid body. Add a chamfer to the edge, and create a circular sketch on the same plane with a slightly larger bounding box, then extrude it 85mm to form another separate body.
