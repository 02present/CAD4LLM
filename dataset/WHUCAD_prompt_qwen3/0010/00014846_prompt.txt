Create a base shape by extruding a sketch on the XY plane with five lines, then create another identical extrusion slightly smaller and offset, followed by a chamfer on the edges. Add a third extrusion using a new sketch with four lines on the same plane to form a final feature.
