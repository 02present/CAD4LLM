Create a base shape by extruding a square sketch with four lines on the XY plane, then create a second extrusion using a slightly smaller square sketch to form a larger structure. Add six small circular sketches around the edges of the base and extrude them to form protrusions.
