Create a base shape by sketching four lines on the XY plane forming a square with 95mm sides and extruding it 12mm. Then, create two identical circles on the XY plane with diameters matching the square's side length and extrude them 8mm each to form separate features.
