Create a base shape by sketching a circle on the XY plane with a bounding box from 0 to 96 in X and -48 to 48 in Y, then extrude it 24 millimeters to form a solid body. Next, chamfer the edges of the extruded feature. Finally, add a second sketch on the XY plane containing an arc and a line within a smaller bounding box and use it to create another feature.
