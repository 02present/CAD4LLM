Create a base shape by sketching three lines and an arc on the XY plane within a 95x95 mm bounding box. Revolve this sketch around the Z-axis to form a symmetrical 3D shape. Then, create a second sketch with eight lines on the XY plane within a slightly smaller bounding box and extrude it 80 mm to form a separate body.
