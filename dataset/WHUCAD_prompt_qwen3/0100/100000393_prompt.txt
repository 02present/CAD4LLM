Create a base shape by sketching eight lines on the XY plane within the specified bounds, then revolve that sketch around an axis to form a symmetrical form. Next, add a smaller sketch with four elements, including an arc, and extrude it 43 millimeters to create a separate body.
