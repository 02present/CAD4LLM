Create a base shape by extruding a sketch on the XY plane with six lines, then add a second extrusion using a smaller sketch to form a taller section. Next, create a larger extruded body from another sketch and apply chamfers to two of the edges. Finally, add additional extrusions for smaller features and chamfer the remaining edges for a finished look.
