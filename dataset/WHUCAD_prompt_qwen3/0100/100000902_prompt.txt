Create a base shape by extruding a sketch with four lines on the XY plane, then add another extrusion using an eight-entity sketch with lines and an arc, followed by a third extrusion using a sketch with two arcs and two lines. Finally, revolve the last sketch around the Z-axis to complete the model.
