Create a base feature by extruding a sketch consisting of four lines on the XY plane, then add another extrusion using a sketch with three lines on the XY plane, followed by an extrusion based on a sketch containing four lines and an arc, all with a depth of 3 millimeters.
