Create a base shape by sketching ten lines on the XY plane within the specified bounds, then extrude this sketch 61 millimeters to form a new body. Next, create a second sketch on the XY plane with five elements, including an arc and four lines, within the new bounding box, and use it as a reference for further operations.
