Create a base shape by extruding a sketch on the XY plane with five lines spanning 6mm in width and 95mm in height, then chamfer the edges. Next, create a second extruded feature using a separate sketch on the XY plane with four lines arranged in a 95mm by 6mm dimension, both extrusions extending 96mm in depth.
