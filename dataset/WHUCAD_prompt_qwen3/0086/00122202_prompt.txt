Create a base body by extruding a sketch along the Z-axis for 32 mm, then create two more identical bodies by extruding two additional sketches along the same axis for the same distance. All sketches are lines forming a rectangular shape on the XY plane with consistent dimensions.
