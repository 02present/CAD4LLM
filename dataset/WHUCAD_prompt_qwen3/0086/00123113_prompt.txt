Create a base shape by extruding a sketch on the XY plane with four lines, then repeat the process with another identical sketch to form a second body. Add a chamfer to one of the edges and create additional extrusions using larger sketches to build up the final structure.
