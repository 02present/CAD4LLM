Create a base shape by extruding a sketch on the XY plane with a 95mm width and 48mm height, then add a chamfer. Next, create a second extrusion using a slightly taller sketch on the same plane with a 16mm depth. Finally, make a third extrusion using a smaller sketch with a 48mm depth to form the final structure.
