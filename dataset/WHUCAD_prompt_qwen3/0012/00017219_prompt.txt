Create a base shape by extruding a sketch on the XY plane with four lines, then add three more similar extrusions using different sketches on the same plane but with varying dimensions. Finally, chamfer one of the edges to refine the overall form.
