Create a base body by extruding a sketch on the XY plane with a 95x63mm bounding box, then add two more extruded features using separate sketches on the same plane, each with a 95x71mm bounding box, and chamfer one of the edges.
