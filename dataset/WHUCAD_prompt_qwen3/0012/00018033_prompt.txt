Create a base shape by extruding a sketch consisting of four lines on the XY plane with a bounding box of 95mm by 56mm, then extrude a circular sketch 3mm to form a separate body. Add additional extrusions based on subsequent sketches, each extending 3mm or 15mm depending on the specified distance, all on the XY plane with varying bounding boxes.
