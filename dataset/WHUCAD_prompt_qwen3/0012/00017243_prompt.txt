Create a base shape by extruding a sketch on the XY plane with a 96 mm depth. Then, chamfer two edges of the resulting body. On the same plane, create a smaller sketch and extrude it 24 mm to form a secondary feature.
