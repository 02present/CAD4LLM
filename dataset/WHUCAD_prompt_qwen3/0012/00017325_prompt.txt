Create a base shape by extruding a sketch on the XY plane with four lines, then repeat this process using another sketch with similar dimensions but slightly different bounding box, and finally add a chamfer to the edges. Continue by creating additional extrusions from new sketches placed in different positions, ensuring each extrusion has the specified depth.
