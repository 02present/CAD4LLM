Create a base shape by extruding a sketch on the XY plane with four lines, then add two chamfers to the edges. Next, create a second extrusion using a new sketch with six lines on the same plane, followed by a third extrusion using another sketch with seven lines, all extending 67 mm deep, and one extrusion at 45 mm.
