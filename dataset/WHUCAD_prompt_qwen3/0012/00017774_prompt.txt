Create a base shape by extruding a sketch on the XY plane with six lines, then add another extrusion using a new sketch with four lines, followed by a chamfer. Continue by extruding three more sketches, each with increasing height, to form a layered structure.
