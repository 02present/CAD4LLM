Create a base shape by extruding a sketch on the XY plane with a depth of 64 mm, then add two chamfers to the edges. Next, create a second sketch on the same plane and extrude it with a depth of 16 mm to form a separate feature.
