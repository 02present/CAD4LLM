Create a base shape by extruding a sketch on the XY plane with four lines, then repeat this process using different sketches on the same plane to form multiple protrusions. Add chamfers to the edges of each extruded feature to round off the sharp corners. Ensure all extrusions are set to a depth of 39 millimeters except the last one, which should be 99 millimeters deep.
