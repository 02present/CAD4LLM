Create a base shape by extruding a sketch on the XY plane with a 64 mm depth, then add a chamfer. Next, create a second extruded feature using a smaller sketch on the XY plane shifted down by 48 mm, extruding it 48 mm deep.
