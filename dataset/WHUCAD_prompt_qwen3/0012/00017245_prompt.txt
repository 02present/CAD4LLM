Create a base shape by extruding a sketch on the XY plane with four lines, then repeat this process using different sketches on the same plane to form multiple stacked bodies. Add chamfers to two specific edges and ensure all extrusions are set to a depth of 96 millimeters.
