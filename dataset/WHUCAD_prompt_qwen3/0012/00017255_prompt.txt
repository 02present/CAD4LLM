Create a base shape by extruding a sketch on the XY plane with a 48mm by 95mm bounding box, then add a chamfer. Next, create a second extruded feature using a slightly larger sketch on the same plane, with a 57mm width, and extrude it 55mm.
