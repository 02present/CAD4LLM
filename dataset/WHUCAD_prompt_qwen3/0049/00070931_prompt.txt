Create a base shape by extruding a sketch on the XY plane with six lines, then repeat this process using the same sketch to form multiple stacked bodies. Add chamfers to two specific edges and create additional extrusions from smaller sketches placed at different positions and sizes.
