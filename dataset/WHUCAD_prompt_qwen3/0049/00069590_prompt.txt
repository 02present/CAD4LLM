Create a base shape by extruding a sketch on the XY plane with a 95mm by 66mm bounding box, then chamfer the edges. Add a second extrusion using a smaller sketch on the same plane with a 95mm by 38mm bounding box, extending 104mm to form a separate body.
