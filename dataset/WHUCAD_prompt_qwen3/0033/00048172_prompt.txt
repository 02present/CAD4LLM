Create a base shape by extruding a sketch on the XY plane with four lines, then repeat this process using different sketches with varying line counts and dimensions, ensuring each extrusion is blind and set to 96mm, except the last ones which are 72mm. Add chamfers at specified locations after each extrusion.
