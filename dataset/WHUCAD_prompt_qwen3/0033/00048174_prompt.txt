Create a base shape by extruding a sketch on the XY plane with five lines spanning 71mm wide and 95mm tall, then extrude it 24mm to form the first body. Add another extrusion of 48mm using a smaller sketch on the same plane, followed by two chamfers on adjacent edges. Repeat this process with varying sketch sizes and extrusion distances to build up the final complex form.
