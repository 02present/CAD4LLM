Create a base shape by extruding a sketch on the XY plane with four lines, then build up the model by repeatedly extruding similar sketches at 90mm each, adding a chamfer after the first extrusion and finishing with an arc-based extrusion at 72mm.
