Create a base shape by sketching eight lines on the XY plane with a bounding box from 0 to 63 in the X direction and -32 to 95 in the Y direction, then extrude it 96 millimeters to form a new body. Next, sketch another set of eight lines on the XY plane within a smaller bounding box and extrude it 48 millimeters. Finally, sketch three lines on the XY plane with a larger bounding box and extrude it 32 millimeters to create the final part.
