Create a base shape by extruding a sketch along the XY plane with a depth of 34 mm, then create another identical extrusion adjacent to it, and finally add two chamfers to the edges of the first extrusion. Additionally, create a smaller sketch at the top and extrude it downward to form a third protruding feature.
