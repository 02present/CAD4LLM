Create a base shape by extruding a sketch on the XY plane with a 70 mm depth, then add a chamfer to the edge. Next, create a second sketch slightly smaller than the first and extrude it 93 mm to form a separate body.
