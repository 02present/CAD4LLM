Create a base shape by extruding a sketch on the XY plane with dimensions 71mm by 95mm, then chamfer the edges. Add a second extrusion of 48mm using a smaller sketch, and revolve three additional features around the Z-axis based on subsequent sketches.
