Create a base shape by extruding a sketch on the XY plane with a 24 mm depth, then add a chamfer to the edge. Next, create another sketch slightly larger than the first and extrude it similarly to form a second protrusion.
