Create a symmetrical base shape by extruding three identical sketches along the Z-axis, each 14 mm tall, and add a circular protrusion on one side by extruding a circle 14 mm high. Then, create a final extruded feature using a larger sketch that spans the full width and height of the base.
