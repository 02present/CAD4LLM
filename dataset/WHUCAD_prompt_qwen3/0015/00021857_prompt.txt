Create a base shape by extruding a sketch on the XY plane with seven lines, then add three chamfers to the edges. Next, extrude four additional sketches on the XY plane to form smaller features, each extending 2 millimeters. Finally, apply chamfers to the remaining edges for a finished look.
