Create a base shape by extruding a sketch of five lines on the XY plane to a depth of 83 mm. Then, chamfer the edges. Add a circular sketch in the XY plane centered below the base and extrude it upward by 22 mm to form a separate feature.
