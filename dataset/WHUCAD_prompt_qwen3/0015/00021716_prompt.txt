Create a base shape by extruding a sketch on the XY plane with four lines, then chamfer the edges. Add three more extrusions using separate sketches on the XY plane, each with four lines, and one circular sketch, all extruded 14 millimeters. Ensure each extrusion creates a new body.
