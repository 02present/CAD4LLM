Create a base body by extruding a circle sketch along the Z-axis for 96 mm. Then chamfer the edge. Next, sketch three lines on the XY plane and extrude them upward for 104 mm to form a separate feature.
